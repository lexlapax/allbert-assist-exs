# Allbert v0.03 Agent Skills Substrate

## Purpose

v0.03 should make Allbert a skills-compatible agent client by adding the
substrate for standard `SKILL.md` discovery, parsing, validation, trust, and
progressive disclosure.

The origin note wants Allbert to find skills, explain which capability it would
use, ask for permission when needed, and eventually help create new skills. The
vision turns that into an Elixir/Jido shape: agents decide, Jido actions perform
bounded side effects, permissions are checked at the action boundary, and
skills remain readable bundles around those capabilities.

The broader skill ecosystem already has an emerging standard. Agent Skills are
directories containing a required `SKILL.md` file with YAML frontmatter and
markdown instructions. They can also include `scripts/`, `references/`, and
`assets/`, and clients are expected to use progressive disclosure: load only
name and description during discovery, load the full skill body on activation,
and load bundled resources only when needed.

Allbert should adopt that format rather than inventing an incompatible
`allbert-skills` format. v0.03 is the foundation layer: parse standard Agent
Skills, validate them, track trust and source, inventory resources, and activate
instructions as context. Optional Allbert metadata can be parsed and reported,
but using it to drive Jido action execution belongs in v0.06 after v0.04
Runtime Convergence and v0.05 Security Central.

This is still not autonomous skill creation, arbitrary code loading, or script
execution. It is the compatibility layer that makes those future features safer.

## External Grounding

The v0.03 design is informed by:

- Agent Skills overview and specification: `https://agentskills.io/home` and
  `https://agentskills.io/specification`
- Agent Skills client implementation guidance:
  `https://agentskills.io/client-implementation/adding-skills-support`
- Agent Skills creator guidance:
  `https://agentskills.io/skill-creation/best-practices`
- Agent Skills script guidance:
  `https://agentskills.io/skill-creation/using-scripts`
- skills.sh catalog, CLI, and API docs: `https://skills.sh/`,
  `https://skills.sh/docs`, and `https://skills.sh/docs/api`

Research consequence: standard Agent Skills support scripts and tool
instructions, but the client decides how activation and execution work. Allbert
should use a dedicated `activate_skill` action instead of raw model file reads
so it can enforce trust, wrap instructions, list resources, redact paths when
needed, and write trace metadata. Shell/script execution remains outside
v0.03.

## Why This Is Next

v0.01 proved the local assistant loop:

- signal-first runtime boundary
- primary intent agent
- named Jido actions
- central permission gate
- markdown memory
- markdown traces
- shared CLI and LiveView entrypoints

The next tempting features are richer memory, scheduled jobs, channels,
external network access, and shell execution. Each of those depends on a common
capability contract. Without it, jobs, channels, and agents would each need
their own way to discover skills, activate instructions, reason about
permissions, and explain what was run.

So v0.03 should build the first of several layers:

- Agent Skills compatibility, so Allbert can participate in the wider skill
  ecosystem.

v0.04 should then make the existing runtime boundary more consistent:

- Jido Runtime Convergence, so runtime-facing settings, skills, memory, trace,
  and future security behavior enter through signals, agents, and registered
  Jido actions while pure helper modules stay plain Elixir.

v0.05 should then add the shared security evaluation layer:

- Security Central, so later actions, confirmations, execution, jobs, channels,
  and memory workflows share policy, risk, redaction, trust, audit, and trace
  vocabulary.

v0.06 should then add the second skill execution layer:

- Allbert capability translation, so selected skills can bind safely to Jido
  actions, Security Central decisions, traces, and memory effects.

The boundary is deliberately sharp:

- v0.03 imports and activates skills as portable manifests, instructions, and
  resource inventories.
- v0.06 turns only trusted Allbert capability contracts into calls to known
  Elixir/Jido actions through the v0.04 action boundary, Security Central, and
  the action runner.

## Current Baseline

- `AllbertAssist.Skills` is a static in-code list of v0.01 capabilities.
- v0.02 provides settings-backed scan paths, trust decisions, and enablement
  defaults for skills.
- `AllbertAssist.Agents.IntentAgent` hard-codes the available action modules in
  `action_modules/0` and deterministic route handling.
- `list_skills` and `read_skill` read from the static skill list.
- `AllbertAssist.Runtime.signal_types/0` already names
  `allbert.action.requested` and `allbert.action.completed`, but v0.01 does
  not yet make those signals the standard action lifecycle path.
- `AllbertAssist.Trace` records selected action metadata, but it does not yet
  record a selected skill manifest, skill version, or skill declaration path.
- `AllbertAssist.Memory` already has a `skills` category, but current durable
  memory entries are append-style records rather than stable skill
  declarations.

## Readiness Decision

v0.03 is implementation-ready after this planning pass. The implementer should
not make new product decisions about format, execution, trust, precedence,
settings, built-in skill names, activation behavior, or closeout tests. If an
implementation detail conflicts with external Agent Skills compatibility, keep
the local runtime safe and record the compatibility warning in diagnostics.

## Core Decision

Allbert should use the Agent Skills format as both its import/export format and
its native authoring format.

An installed skill is a directory with `SKILL.md` and optional supporting
folders. Allbert parses that into an internal `AllbertSkill` record. If the
skill includes Allbert metadata, v0.03 should preserve and validate it enough to
show diagnostics, but it should not use that metadata to execute actions yet.

`SKILL.md` is executable authority in neither v0.03, v0.04, nor v0.05. It is a
portable manifest and instruction bundle. Executable Allbert behavior lives in
registered Elixir/Jido actions. The Allbert metadata overlay is a binding
contract from a trusted skill to those registered actions, not runtime code and
not a request to compile or load a module.

Allbert-specific skills should therefore be standard Agent Skills plus a small
namespaced metadata overlay. Prefer fields that stay compatible with the
specification:

```markdown
---
name: append-memory
description: Save user-approved memory into Allbert's markdown memory. Use when the user asks Allbert to remember a fact, preference, or project note.
compatibility: Allbert v0.03+. Requires the built-in append_memory Jido action.
allowed-tools: allbert:action:append_memory
metadata:
  allbert.kind: capability
  allbert.version: "0.3.0"
  allbert.actions: append_memory
  allbert.permissions: memory_write
  allbert.confirmation: not_required
  allbert.memory-effects: writes_markdown_memory
  allbert.trace-effects: records_selected_skill,records_permission_decision
---

## Workflow

1. Confirm the memory is user-approved or low-risk under Allbert's memory
   policy.
2. Use the `append_memory` Allbert action.
3. Report the markdown path and category.
```

Notes:

- Agent Skill names use hyphens. Allbert action names may remain snake case.
- The frontmatter `metadata` map should use namespaced string keys because the
  Agent Skills spec reserves only a small set of core fields.
- `allowed-tools` is treated as advisory and experimental. It must never grant
  permission by itself.
- If the ecosystem later standardizes richer permissions, Allbert can translate
  to or from those fields without changing the directory format.
- Future skill creation may scaffold both `SKILL.md` and Elixir action code,
  but generated code must be reviewed, tested, and compiled as normal app code.

## Skill Kinds

The registry should classify skills into these kinds:

- `:instruction`: standard Agent Skill with no Allbert action metadata. It can
  be discovered, read, and activated as context. It cannot run new side effects.
- `:workflow`: instruction skill with checklists, references, templates, or
  scripts. In v0.03, scripts are inspectable resources only.
- `:capability_candidate`: skill whose Allbert metadata appears to map to Jido
  actions and permission classes, but is inert in v0.03 and is not executable
  until v0.06 validates it against registered actions.
- `:native_action`: built-in Allbert/Jido capability represented as an Agent
  Skill wrapper for user-facing discovery and activation. In v0.03 this is
  descriptive; v0.06 wires it to action execution.
- `:external_candidate`: skill discovered through skills.sh or another
  untrusted/import source but not trusted, enabled, or executable.

This distinction lets Allbert import ecosystem skills without pretending every
skill is safe executable code.

## Non-Goals

- No autonomous skill creation.
- No arbitrary code loading from skill files.
- No scripting engine.
- No auto-generating, compiling, or loading Elixir modules from skill folders.
- No shell command execution.
- No bundled skill script execution.
- No external network calls.
- No embeddings or vector search.
- No additional user channels.
- No skill-driven Jido action execution.
- No action lifecycle runner.
- No generic skill creation helper beyond documentation and examples.
- No automatic `npx skills add` or skills.sh API calls without explicit
  external-network confirmation.
- No scheduled job runtime beyond what is needed to keep the registry design
  compatible with future jobs.

## Discovery Scopes

Allbert should scan multiple scopes with deterministic precedence and trust
metadata:

- Built-in: packaged with the app, for example
  `apps/allbert_assist/priv/skills/`.
- Project native: `./.allbert/skills/`.
- Project interoperable: `./.agents/skills/`.
- User native: `<ALLBERT_HOME>/skills/`, defaulting to
  `~/.allbert/skills/`.
- User interoperable: `~/.agents/skills/`.
- Imported cache: `<ALLBERT_HOME>/cache/skills/` for skills imported from
  skills.sh, git, or a local path after user confirmation.

Project skills should be gated by a trust decision before they can affect the
agent catalog. A newly cloned repository should not be able to silently inject
instructions into Allbert.

User-owned and imported skills should resolve through `AllbertAssist.Paths` so
they follow the same Allbert Home rules as settings, secrets, memory, and the
local database. Project-local `./.allbert/skills/` remains a separate
trust-gated scope because it is owned by the current workspace, not by the
operator's global Allbert home.

The existing markdown memory `skills` category should not become the install
location for executable skill folders. Use memory for skill notes, activation
traces, review records, and user preferences about skills. Installed skills
should remain ordinary directories.

Discovery path contract:

- Built-in skills are trusted and enabled by default.
- User native skills under `<ALLBERT_HOME>/skills/` are operator-owned and
  trusted by default, unless disabled by settings.
- User interoperable skills under `~/.agents/skills/` are operator-owned and
  trusted by default, unless disabled by settings.
- Imported cache skills under `<ALLBERT_HOME>/cache/skills/` are disabled by
  default in v0.03, regardless of parse validity.
- Project native and project interoperable skills are pending until the current
  project root appears in `skills.trusted_project_roots`.
- Duplicate names never crash registry loading. Built-in names are reserved in
  v0.03. Non-built-in duplicates resolve in this order:
  trusted project native, trusted project interoperable, user native, user
  interoperable, enabled imported cache.
- Hidden duplicates and pending project skills must appear in diagnostics but
  not in the model-facing catalog.

Settings changes required in v0.03:

- Make these existing v0.02 keys writable through Settings Central:
  `skills.scan_paths`, `skills.trusted_project_roots`, `skills.enabled`,
  `skills.disabled`, and `skills.imported_cache_policy`.
- Validate `skills.scan_paths`, `skills.trusted_project_roots`,
  `skills.enabled`, and `skills.disabled` as string lists.
- Validate `skills.imported_cache_policy` as one of `disabled`,
  `enabled_manual_trust`.
- Do not add credential settings or external API settings for skills.sh in
  v0.03.

## Translation Model

The runtime should follow this pipeline:

```text
Skill directory
  -> SKILL.md parser
  -> AgentSkillSpec
  -> AllbertSkill registry record
  -> trusted model-facing catalog
  -> activate_skill action
  -> wrapped instructions plus resource inventory
  -> trace metadata
```

Suggested internal structs:

- `AllbertAssist.Skills.AgentSkillSpec`
  - source path
  - name
  - description
  - license
  - compatibility
  - allowed tools
  - metadata
  - markdown body
  - resource inventory
- `AllbertAssist.Skills.Skill`
  - normalized name
  - title/display name
  - source scope
  - trust status
  - kind
  - activation mode
  - original spec
  - diagnostics
- `AllbertAssist.Skills.CapabilityContract`
  - action names
  - permission classes
  - confirmation policy
  - memory effects
  - trace effects
  - script/resource policy

Public facade contract:

- `AllbertAssist.Skills.list(context \\ %{})`
  returns `{:ok, [Skill.t()]}` for trusted/enabled model-facing skills.
- `AllbertAssist.Skills.get(name, context \\ %{})`
  returns `{:ok, Skill.t()}` or `{:error, :not_found}`.
- `AllbertAssist.Skills.read(name, context \\ %{})`
  returns `{:ok, %{skill: Skill.t(), body: String.t(), diagnostics: list()}}`
  or `{:error, :not_found}`.
- `AllbertAssist.Skills.activate(name, context \\ %{})`
  returns `{:ok, activation_map}` or `{:error, reason}`.
- `AllbertAssist.Skills.diagnostics(context \\ %{})`
  returns `{:ok, [map()]}` with invalid, pending, hidden duplicate, disabled,
  and compatibility-warning entries.

Use a small facade so existing actions can move from static declarations to
registry-backed data without changing every call site at once.

In v0.03, capability contracts are drafts. The registry validates them enough to
surface diagnostics against:

- known Jido action names
- known permission classes from `PermissionGate.permission_classes/0`
- supported confirmation policies
- resource and script policy
- trust status
- duplicate name precedence

v0.04 defines the common action/runtime boundary. v0.05 defines how security
decisions are evaluated and audited. v0.06 decides how valid draft contracts
become executable through the action runner.

The v0.06 execution pipeline should be documented as:

```text
activated/trusted skill
  -> Allbert metadata contract
  -> Security Central context and decision
  -> action runner
  -> registered Jido action
  -> result plus action lifecycle signals
  -> trace
```

## Registry Rules

- Built-in skills should ship with the app as stable markdown declarations.
- User-owned skills should live in Agent-Skills-compatible directories, not in
  opaque database records.
- Every `SKILL.md` must validate the required Agent Skills frontmatter before
  it enters the catalog.
- The model-facing catalog should include only enabled and trusted skills.
- A capability candidate may reference only known Allbert action names.
- A skill may not name an arbitrary Elixir module and cause it to execute.
- A skill may not generate, compile, or load arbitrary Elixir code.
- A skill may not execute bundled scripts in v0.03.
- `allowed-tools` and `metadata.allbert.*` are hints to the registry; the
  current permission gate remains the v0.03 source of truth.
- Invalid or untrusted skill files should be visible in registry diagnostics
  without crashing the assistant loop.
- Duplicate skill names should resolve deterministically. Project-level skills
  may override user-level skills only when the project is trusted; otherwise
  they should be hidden or marked pending.

Naming rules:

- Canonical Agent Skill names use kebab case, for example `append-memory`.
- Public reads and activation may accept snake case aliases, for example
  `append_memory`, but registry records store canonical kebab names.
- Valid canonical names match `^[a-z0-9][a-z0-9-]{1,80}$`.
- Built-in wrapper names are:
  - `direct-answer`
  - `append-memory`
  - `read-recent-memory`
  - `list-skills`
  - `read-skill`
  - `plan-shell-command`
  - `external-network-request`

Parser rules:

- `SKILL.md` must start with YAML frontmatter delimited by `---`.
- Frontmatter must parse to a map with string keys.
- `name` and `description` are required non-empty strings.
- `description` should be at most 1024 characters; longer descriptions load
  with a compatibility warning and are truncated in catalog output only.
- Preserve markdown body exactly except for normal file decoding.
- Preserve unknown top-level frontmatter fields in `metadata.external_fields`
  for diagnostics; do not treat them as Allbert permissions.
- `metadata` must remain a map if present. Unknown `metadata.allbert.*` keys
  produce warnings, not failures.
- Missing `SKILL.md`, unparseable YAML, missing name, or missing description
  skips the skill and produces diagnostics.

Resource inventory rules:

- Inventory only files under `scripts/`, `references/`, and `assets/`.
- Store paths relative to the skill directory, file kind, byte size, and a
  content hash.
- Skip hidden files and directories by default.
- Do not read resource file contents during catalog loading.
- Do not execute resources in v0.03.

## Activation Model

Allbert should implement progressive disclosure explicitly through a dedicated
`activate_skill` action:

- Catalog: at runtime startup or first use, load only skill name, description,
  scope, trust state, and kind.
- Activation: when the user or intent agent selects a trusted skill, call
  `activate_skill`; load the body of `SKILL.md`; and return it wrapped as skill
  context with source, trust, diagnostics, and resource inventory metadata.
- Resources: enumerate `scripts/`, `references/`, and `assets/` during
  activation, but do not eagerly load them. Load a resource file only when a
  selected skill's instructions call for it and the permission model allows
  reading it. Never execute resources in v0.03.

Model-driven activation can remain deterministic in v0.03 through the internal
`activate_skill` action selected by explicit prompts. More autonomous
model-driven selection belongs after the registry and trust model are stable.
User-explicit activation should be supported through plain prompts at first, and
later through CLI or UI syntax such as `/skill append-memory` once the
interface design catches up.

For v0.03, activation changes context and trace metadata. It should not grant
new execution powers.

Activation output contract:

```elixir
%{
  name: "append-memory",
  title: "Append Memory",
  kind: :native_action,
  source_scope: :built_in,
  trust_status: :trusted,
  instructions: "...markdown body...",
  resource_inventory: [%{path: "references/example.md", kind: :reference}],
  capability_contract: %{status: :draft, actions: ["append_memory"]},
  diagnostics: [],
  permission_decision: %{permission: :read_only, decision: :allowed}
}
```

The action result should include an `actions` entry with:

- `name: "activate_skill"`
- `status: :completed | :not_found | :denied | :invalid`
- `permission: :read_only`
- `selected_skill`
- `skill_metadata` containing source scope, trust status, kind, diagnostics,
  and resource inventory summary

Trace rendering should add a `Skill metadata` section rather than burying skill
details only inside inspected action maps.

## skills.sh And Import Policy

skills.sh matters in two roles:

- discovery catalog: search, leaderboard, install counts, official sources, and
  detail pages
- risk signal: audit endpoints can return pass/warn/fail status and risk
  levels from security partners

Allbert should not depend on skills.sh for local operation. v0.03 should work
with local folders and skills already installed by other tools. skills.sh
integration can be documented in v0.03 but should remain design-only for the
implementation. Search, detail fetch, audit fetch, and import require external
network access or file writes, so they belong behind later confirmation-gated
adapters.

Planned import flow:

1. User asks for a skill or says Allbert needs a capability.
2. Allbert searches local installed skills first.
3. If nothing fits, Allbert can propose an external search.
4. External search uses `Req` and the skills.sh API only after confirmation.
5. Results are ranked by relevance, source reputation, install count, official
   status, duplication status, and audit risk.
6. Installation/import writes to an Allbert-controlled skill cache or delegates
   to `npx skills add` only after explicit user confirmation.
7. Imported skills are disabled until parsed, validated, and trusted.

The Skills CLI is useful for interoperability, but Allbert should not make
Node/npm a hard runtime dependency. The registry should read standard skill
directories directly. A future import adapter can call `npx skills` when
available.

v0.03 does not add a skills.sh Mix task or network action. It may document where
externally installed skills are scanned.

## Future Allbert Skill Creation Rule

Allbert-specific skills should eventually be created as Agent Skills first.
This is a design rule for v0.06 and later, not a v0.03 implementation
requirement. The creation workflow should be:

1. Start from a real repeated task, trace, correction, or user preference.
2. Decide whether the skill is instruction-only, workflow, or action-backed.
3. Draft a standard `SKILL.md` with a precise description that says when to use
   it.
4. Add Allbert metadata only if the skill needs known Jido actions or permission
   semantics.
5. If a new side effect is needed, create the Elixir `Jido.Action` and
   permission contract first; then create the skill wrapper.
   Do not make the skill's script or markdown body the executable substrate.
6. Add small test prompts:
   - should activate
   - should not activate
   - should refuse or stay read-only
7. Run the skill against the tests and inspect traces.
8. Iterate the description and instructions based on actual false positives,
   false negatives, failures, and user corrections.
9. Promote the skill from draft to trusted only after validation passes.

This gives Allbert a path to self-improving skills later: traces and memory can
suggest a skill draft, but the runtime still validates and asks before enabling
it.

## Implementation Order

### Milestone 1: Compatibility Decision And ADR

Status: complete.

Implementation tasks:

- Record the decision to adopt Agent Skills as Allbert's import/export and
  native authoring format.
- Document the Allbert metadata overlay and translation rules.
- Document discovery scopes, trust precedence, and duplicate handling.
- Document the execution boundary: Agent Skills are portable instructions;
  Allbert execution is registered Elixir/Jido action code.
- Document exact settings keys, trust policy, duplicate precedence, public
  facade API, and built-in skill names.
- Confirm the v0.02 direct YAML parser dependency supports `SKILL.md`
  frontmatter.
- Add or update `docs/plans/v0.03-request-flow.md` with the planned discovery,
  parse, registry, activation, and trace flows.

Implementation notes:

- `docs/adr/0003-skill-manifests-as-capability-contracts.md` is the binding
  compatibility decision: Agent Skills are the external/native format, the
  Allbert metadata overlay is inert in v0.03, and execution is constrained to
  registered Elixir/Jido actions.
- `docs/plans/v0.03-request-flow.md` defines the planned discovery scopes,
  trust rules, parser behavior, registry facade, `activate_skill` boundary, and
  trace expectations for the remaining milestones.
- The existing v0.02 YAML parser dependency is retained as the frontmatter
  parser basis for `SKILL.md`.

Acceptance criteria:

- The plan and ADR say clearly that Allbert is skills-compatible.
- No separate incompatible allbert-only skill format is introduced.
- The docs say that skill folders do not auto-generate or load Elixir modules.
- `docs/plans/v0.03-request-flow.md` has the planned flow and operator checks.

### Milestone 2: Agent Skill Parser And Validator

Status: complete.

Implementation tasks:

- Add a parser for `SKILL.md` YAML frontmatter and markdown body.
- Add structs for `AgentSkillSpec`, resource inventory entries, and parser
  diagnostics.
- Validate required Agent Skills fields:
  - `name`
  - `description`
- Validate Agent Skills naming constraints as diagnostics:
  - missing or empty `description` skips the skill
  - unparseable YAML skips the skill
  - invalid names, oversized descriptions, and parent-directory mismatches
    produce compatibility warnings when the skill can still be safely loaded
- Preserve optional fields:
  - `license`
  - `compatibility`
  - `metadata`
  - `allowed-tools`
- Inventory optional resource directories:
  - `scripts/`
  - `references/`
  - `assets/`
- Add registry diagnostics for malformed YAML, missing fields, oversized
  descriptions, invalid names, and parent-directory mismatches.
- Add test fixtures for valid standard, valid Allbert metadata, invalid YAML,
  missing required fields, duplicate names, resources, and scripts.

Implementation notes:

- `AllbertAssist.Skills.Parser.parse_dir/1` parses one Agent Skill directory
  into `AllbertAssist.Skills.AgentSkillSpec` or returns fatal diagnostics.
- `AllbertAssist.Skills.Parser.parse_many/1` parses multiple directories and
  reports aggregate diagnostics, including duplicate names.
- `AllbertAssist.Skills.Resource` records resource path, kind, byte size, and
  SHA-256 digest for files under `scripts/`, `references/`, and `assets/`.
- Parser diagnostics use maps with `:severity`, `:code`, `:message`, and
  optional `:path`, `:field`, and `:value`.
- Allbert metadata is preserved as inert frontmatter context. Unknown Allbert
  metadata and unknown top-level frontmatter fields warn but do not grant any
  permission or execution authority.
- Scripts are inventoried only and receive an informational inert-script
  diagnostic.
- Test fixtures live under
  `apps/allbert_assist/test/support/fixtures/skills/`.

Acceptance criteria:

- A valid standard Agent Skill parses without Allbert-specific metadata.
- A valid Allbert capability skill parses with namespaced metadata.
- Invalid skills are reported as diagnostics rather than exceptions.
- Non-fatal ecosystem deviations load only with visible compatibility
  warnings.
- Resource directories are inventoried without reading or executing resources.

Operator test:

```sh
mix test apps/allbert_assist/test/allbert_assist/skills/parser_test.exs
```

Expected behavior: valid fixtures parse; invalid fixtures return diagnostics;
script files are listed only as resources.

Verification on 2026-05-02:

```sh
mix test apps/allbert_assist/test/allbert_assist/skills/parser_test.exs
```

Result: 7 tests, 0 failures.

### Milestone 3: Discovery, Trust, And Registry

Status: complete.

Implementation tasks:

- Add registry modules, tentatively:
  - `AllbertAssist.Skills.Registry`
  - `AllbertAssist.Skills.Skill`
  - `AllbertAssist.Skills.CapabilityContract`
- Reuse the M2 parser modules:
  - `AllbertAssist.Skills.Parser`
  - `AllbertAssist.Skills.AgentSkillSpec`
  - `AllbertAssist.Skills.Resource`
- Scan built-in, project, user, and interoperable skill paths.
- Track source scope, path, trust status, enabled/disabled status, and
  diagnostics.
- Implement deterministic duplicate handling.
- Add settings validation/write support for the v0.03 `skills.*` keys.
- Keep `AllbertAssist.Skills` as a small facade so existing actions keep a
  stable call site.

Implementation notes:

- `AllbertAssist.Skills.Registry` is stateless and scans built-in, project,
  user, interoperable, imported-cache, and configured scan roots.
- `AllbertAssist.Skills.Skill` stores normalized registry records with source,
  trust, kind, activation mode, aliases, diagnostics, and inert contract data.
- `AllbertAssist.Skills.CapabilityContract` preserves Allbert metadata as
  non-executable draft or legacy contract data.
- `AllbertAssist.Skills` now delegates list/get/read/diagnostics to the
  registry. `activate/2` remains unavailable until M5.
- `list_skills` and `read_skill` now use registry-backed data.
- Project skills are pending until `skills.trusted_project_roots` includes the
  project root.
- Imported cache skills require `skills.imported_cache_policy:
  enabled_manual_trust` and an entry in `skills.enabled`.
- Built-in names are reserved and non-built-in duplicates are hidden with
  diagnostics.
- M3 keeps a temporary `:built_in_legacy` bridge for the current v0.01
  capabilities so operator prompts keep working until M4 seeds real built-in
  `SKILL.md` files.

Acceptance criteria:

- `AllbertAssist.Skills.list/0` returns registry-loaded skills.
- `AllbertAssist.Skills.get/1` reads by name from the registry.
- Project skills are hidden or pending unless trusted.
- Tests cover precedence, trust gating, disabled skills, malformed files, and
  duplicate names.
- Built-in names are reserved and cannot be overridden in v0.03.
- `AllbertAssist.Skills.diagnostics/1` reports pending, disabled, duplicate,
  and invalid declarations.

Operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v003-m3-home
rm -rf "$ALLBERT_HOME"
mix run --no-start -e 'IO.inspect(AllbertAssist.Skills.list()); IO.inspect(AllbertAssist.Skills.diagnostics())'
```

Expected behavior: registry-backed skills are listed; before M4, v0.01
capabilities appear through the temporary `:built_in_legacy` bridge.
Diagnostics are structured and do not crash even with invalid fixtures in
configured temp scan paths.

Verification on 2026-05-02:

```sh
mix test apps/allbert_assist/test/allbert_assist/skills/registry_test.exs apps/allbert_assist/test/allbert_assist/settings_test.exs apps/allbert_assist/test/allbert_assist/actions/intent_actions_test.exs apps/allbert_assist/test/allbert_assist/agents/intent_agent_test.exs apps/allbert_assist/test/allbert_assist/runtime_intent_agent_test.exs
```

Result: 47 tests, 0 failures.

Operator smoke:

```sh
ALLBERT_HOME=/tmp/allbert-v003-m3-home-smoke mix run --no-start -e 'Application.put_env(:allbert_assist, AllbertAssist.Skills.Registry, user_interoperable_root: "/tmp/allbert-v003-m3-home-smoke/agent-skills"); IO.inspect(AllbertAssist.Skills.list()); IO.inspect(AllbertAssist.Skills.diagnostics())'
```

Result: registry list returned trusted legacy built-ins and diagnostics returned
`{:ok, []}` in the disposable home.

### Milestone 4: Built-In Allbert Skill Pack

Status: complete.

Implementation tasks:

- Seed built-in Agent Skill wrappers for the current v0.01 actions:
  - `direct-answer`
  - `append-memory`
  - `read-recent-memory`
  - `list-skills`
  - `read-skill`
  - `plan-shell-command`
  - `external-network-request`
- Use Agent Skill names with hyphens while mapping to snake-case action names.
- Include Allbert metadata as descriptive, non-executable contract data.
- Include instructions that preserve v0.01 safety boundaries.
- Replace the M3 `:built_in_legacy` bridge with real built-in `SKILL.md`
  declarations from `priv/skills`.

Implementation notes:

- Built-in declarations live under `apps/allbert_assist/priv/skills/`.
- The pack includes `direct-answer`, `append-memory`, `read-recent-memory`,
  `list-skills`, `read-skill`, `plan-shell-command`, and
  `external-network-request`.
- Each declaration uses standard Agent Skills frontmatter and inert
  `metadata.allbert.*` fields for action names, permission classes,
  confirmation policy, memory effects, and trace effects.
- The registry now loads normal built-ins with `source_scope: :built_in`; the
  M3 `:built_in_legacy` bridge is only a defensive fallback if no built-in
  declarations are packaged.
- `read_skill` renders inert capability actions and permission classes so
  operators can inspect the metadata without executing anything.
- Intent routing now recognizes "what skills are available?" as a skill-list
  request.

Acceptance criteria:

- Each existing capability has one readable skill declaration.
- The built-in skill pack can be listed and read through the registry.
- No built-in skill grants command execution or external network access.
- Existing prompts that list/read skills continue to work, accepting both
  kebab-case names and old snake-case aliases.

Operator test:

```sh
mix allbert.ask --trace "what skills are available?"
mix allbert.ask --trace "read skill append-memory"
mix allbert.ask --trace "read skill append_memory"
```

Expected behavior: results come from built-in `SKILL.md` declarations and no
script or Jido action is executed.

Verification on 2026-05-02:

```sh
mix test apps/allbert_assist/test/allbert_assist/skills/built_in_pack_test.exs apps/allbert_assist/test/allbert_assist/skills/registry_test.exs apps/allbert_assist/test/allbert_assist/actions/intent_actions_test.exs apps/allbert_assist/test/allbert_assist/agents/intent_agent_test.exs apps/allbert_assist/test/allbert_assist/runtime_intent_agent_test.exs
```

Result: 40 tests, 0 failures.

Operator smoke:

```sh
ALLBERT_HOME=/tmp/allbert-v003-m4-home ALLBERT_TRACE_ENABLED=true mix allbert.ask --trace "what skills are available?"
ALLBERT_HOME=/tmp/allbert-v003-m4-home ALLBERT_TRACE_ENABLED=true mix allbert.ask --trace "read skill append-memory"
ALLBERT_HOME=/tmp/allbert-v003-m4-home ALLBERT_TRACE_ENABLED=true mix allbert.ask --trace "read skill append_memory"
```

Result: list/read used built-in `SKILL.md` declarations and wrote traces under
the disposable Allbert Home.

### Milestone 5: Activation And Progressive Disclosure

Status: complete.

Implementation tasks:

- Add an `activate_skill` action that returns wrapped skill instructions and a
  resource inventory.
- Make `activate_skill` the only v0.03 activation path exposed to the runtime
  and model-facing catalog.
- Update `list_skills` and `read_skill` to use registry data.
- Keep instruction activation separate from action execution.
- Record activated skill metadata in traces.
- Update `AllbertAssist.Trace` to render skill metadata explicitly.

Implementation notes:

- `AllbertAssist.Actions.Intent.ActivateSkill` activates trusted registry
  skills with read-only permission.
- `AllbertAssist.Skills.activate/2` and
  `AllbertAssist.Skills.Registry.activate/2` return wrapped progressive
  disclosure data.
- `IntentAgent` routes `activate skill`, `use skill`, and `load skill`
  requests to `activate_skill`.
- Activation output includes skill context, instructions, resource inventory,
  inert capability contract metadata, diagnostics, and a v0.03 safety boundary.
- Missing, disabled, invalid, or untrusted skills outside the model-facing
  catalog return a structured `:not_found` response.
- `AllbertAssist.Trace` now renders a skill metadata summary line and a
  dedicated `## Skill Metadata` section.

Acceptance criteria:

- "What skills are available?" lists trusted registry skills.
- "Read skill append-memory" displays the standard skill instructions.
- "Activate skill append-memory" returns wrapped skill content and resources.
- Activation does not execute scripts or external tools.
- Activation traces include selected skill, source, trust, diagnostics, and
  resource inventory.
- Activation of invalid, disabled, or untrusted skills returns a structured
  refusal and does not load resource contents.

Operator test:

```sh
export ALLBERT_TRACE_ENABLED=true
mix allbert.ask --trace "activate skill append-memory"
mix allbert.ask --trace "activate skill a-missing-skill"
```

Expected behavior: trusted skill activation returns wrapped instructions and a
trace path; missing skill returns a structured not-found response.

Verification on 2026-05-02:

```sh
mix test apps/allbert_assist/test/allbert_assist/actions/intent_actions_test.exs apps/allbert_assist/test/allbert_assist/agents/intent_agent_test.exs apps/allbert_assist/test/allbert_assist/skills/built_in_pack_test.exs apps/allbert_assist/test/allbert_assist/runtime_intent_agent_test.exs apps/allbert_assist/test/allbert_assist/runtime_test.exs
```

Result: 46 tests, 0 failures.

Operator smoke:

```sh
ALLBERT_HOME=/tmp/allbert-v003-m5-home ALLBERT_TRACE_ENABLED=true mix allbert.ask --trace "activate skill append-memory"
ALLBERT_HOME=/tmp/allbert-v003-m5-home ALLBERT_TRACE_ENABLED=true mix allbert.ask --trace "activate skill a-missing-skill"
```

Result: trusted activation returned wrapped instructions and no resource
execution; missing activation returned `status: not_found`; both traces include
explicit skill metadata.

### Milestone 6: Operator Loop And Documentation

Status: complete.

Implementation tasks:

- Update the request-flow documentation for the registry and activation path.
- Keep `docs/plans/v0.03-request-flow.md` current with actual module names and
  operator verification notes.
- Add operator tests for CLI and LiveView.
- Document how to install third-party skills using external tools and where
  Allbert scans for them.
- Document why skills.sh search/install is confirmation-gated.
- Update the roadmap with v0.03 status, v0.04 as the Runtime Convergence
  milestone, v0.05 as the Security Central milestone, and v0.06 as the
  action-backed skill milestone.

Implementation notes:

- CLI tests cover list, read, snake-case alias read, activation, and trace
  skill metadata through the default runtime.
- LiveView tests cover activation through `AllbertAssist.Runtime`.
- `docs/plans/v0.03-request-flow.md` now documents implemented scan scopes,
  parser behavior, registry behavior, built-in skill files, activation output,
  trace metadata, local third-party skill installation, and skills.sh
  confirmation-gated future work.
- The roadmap marks v0.03 complete, v0.04 as the Runtime Convergence
  milestone, v0.05 as the Security Central milestone, and v0.06 as the next
  action-backed skill milestone.

Acceptance criteria:

- `mix precommit` passes.
- The CLI can list skills, read a skill, activate a skill, and show
  registry/trace metadata.
- The `/agent` LiveView can perform the same loop through the runtime.
- Invalid skill declarations can be inspected without breaking the assistant.
- `docs/plans/v0.03-request-flow.md` matches the implemented modules and
  operator-visible behavior.

Verification on 2026-05-02:

```sh
mix test apps/allbert_assist/test/mix/tasks/allbert_ask_test.exs apps/allbert_assist_web/test/allbert_assist_web/live/agent_live_test.exs
```

Result: 6 tests, 0 failures.

Final gate:

```sh
mix precommit
```

Result: 119 tests, 0 failures; Credo found no issues.

Closeout operator test:

```sh
export ALLBERT_HOME=/tmp/allbert-v003-closeout-home
export ALLBERT_TRACE_ENABLED=true
rm -rf "$ALLBERT_HOME"
mix allbert.ask --trace "what skills are available?"
mix allbert.ask --trace "read skill append-memory"
mix allbert.ask --trace "activate skill append-memory"
mix allbert.ask --trace "activate skill a-missing-skill"
PORT=4016 mix phx.server
```

Then visit:

- `http://localhost:4016/agent`

Expected behavior:

- CLI list/read/activate uses registry-backed skills.
- `/agent` can list, read, and activate the same skills.
- Traces are written under `<ALLBERT_HOME>/memory/traces`.
- Trace files show selected skill, source, trust, diagnostics, and resource
  inventory.
- No skill activation executes scripts, shell commands, network calls, package
  installs, or Jido actions.

## Operator Test Sketch

Use a temporary Allbert Home so user-owned skill declarations, imported caches,
memory, settings, and traces are easy to inspect:

```sh
export ALLBERT_HOME=/tmp/allbert-v003-demo
export ALLBERT_TRACE_ENABLED=true
rm -rf "$ALLBERT_HOME"
```

Then run:

```sh
mix allbert.ask --trace "what skills are available?"
mix allbert.ask --trace "read skill append-memory"
mix allbert.ask --trace "activate skill append-memory"
mix allbert.ask --trace "read skill from an invalid fixture"
```

Expected result:

- Skill list/read/activate output comes from Agent-Skills-compatible
  `SKILL.md` directories.
- Existing v0.01 memory and command behavior remains unchanged.
- Traces show activated skill, source scope, trust status, diagnostics, and
  resource inventory.
- No skill activation executes a script or Jido action.

## Definition Of Done

v0.03 is done when:

- Allbert can parse and validate standard Agent Skills directories.
- Built-in and user/project skills are loaded from predictable scopes with
  trust and precedence rules.
- Allbert-specific capability metadata is a compatibility-preserving overlay,
  not a forked skill format, and remains inert in v0.03.
- Instruction-only skills can be activated without gaining new side effects.
- Traces record selected skill, source, trust, diagnostics, and resources.
- CLI and LiveView expose the same registry-backed list/read/activate behavior.
- No new unsafe execution capability has been added.
- The roadmap is updated with v0.03 completion status, v0.04 Runtime
  Convergence work, v0.05 Security Central work, and v0.06 action-backed skill
  work.
