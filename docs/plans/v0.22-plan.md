# Allbert v0.22 StockSage Python Bridge

Formerly tracked as M-D2b.

## Status

Status: implemented through M5 closeout on 2026-05-15. Ready for operator
manual verification. Release tag `v0.22` is pending operator acceptance.

## Source Documents

- `docs/plans/roadmap.md`
- `docs/plans/allbert-jido-vision.md`
- `docs/plans/v0.20-plan.md`
- `docs/plans/v0.20-request-flow.md`
- `docs/plans/v0.21-plan.md`
- `docs/plans/v0.21-request-flow.md`
- `docs/adr/0006-security-central.md`
- `docs/adr/0007-jido-native-internal-runtime-boundaries.md`
- `docs/adr/0008-durable-confirmation-requests.md`
- `docs/adr/0014-local-workspace-identity.md`
- `docs/adr/0017-allbert-plugin-contract.md`
- `docs/adr/0018-stocksage-local-domain-app.md`
- `docs/adr/0019-cross-surface-intent-enrichment.md`
- `docs/adr/0020-stocksage-python-bridge-protocol.md`

## Purpose

v0.22 brings real analysis results into Allbert through a supervised Python
bridge around the existing TradingAgents baseline. The bridge is transitional:
it proves the execution path that later native Jido agents (now v0.25 after
the project-direction rethink renumbering, formerly v0.23) will reuse
without changing the action, permission, confirmation, or result-persistence
contracts.

By the end of v0.22, a user can ask Allbert to analyze a stock, receive a
confirmation request, approve it, and see a real analysis result stored in
StockSage's local domain records and queryable through existing list/show
actions. This is the first end-to-end StockSage user loop.

## Handoff Note For The Project-Direction Rethink

v0.22 is a single-action execution path. `RunAnalysis` runs one analysis
per confirmation; there is no private objective loop, no per-analysis
queue across confirmations, and no shared work state across turns. That
shape is correct for v0.22 and ships unchanged.

The project-direction rethink
(`docs/plans/project-direction-rethink-01.md`) inserts two new milestones
ahead of native trading agents:

- v0.23 Jido State-Machine Convergence converts
  `AllbertAssist.Confirmations.Store` and `AllbertAssist.Jobs.Scheduler`
  to `Jido.Agent`. v0.22's confirmation flow continues to behave
  identically; only the implementation substrate of `Confirmations.Store`
  changes.
- v0.24 Objective Runtime Foundation adds the durable multi-step work
  substrate. `StockSage.Actions.RunAnalysis` gains optional
  `objective_id`/`step_id` parameters, threaded through confirmations,
  audit, trace, and the `stocksage_analyses` row. v0.22 invocations
  without those parameters continue to work as today (both columns
  are NULL).

v0.22 itself does not change to accommodate v0.23 or v0.24. The handoff
is that future StockSage analyses (v0.24+) can be invoked as objective
steps; existing v0.22 direct invocations remain valid.

## Readiness Decision

v0.22 is ready for implementation when:

- v0.20 StockSage plugin, app, domain tables, queue actions, and safe local
  read actions are implemented and released.
- v0.21 memory review and retrieval is implemented and released.
- ADR 0020 is accepted (to be done in M1).
- `StockSage.Supervisor` exists as the plugin supervisor contributed via
  `StockSage.Plugin.child_spec/1`.
- `stocksage_analyses`, `stocksage_analysis_details`, `stocksage_analysis_queue`,
  and `stocksage_queue_runs` tables exist in the shared Allbert SQLite database.
- `StockSage.Plugin.actions/0` is the registry contribution point for new
  StockSage actions; `allbert_plugin.json` lists existing contributions.
- The v0.19 intent engine is available with `active_app: :stocksage` candidate
  scoring.

## Architecture Readiness Audit

The v0.22 plan is consistent with the current architecture:

- ADR 0006 keeps Security Central as the permission and risk boundary. A new
  `:stocksage_analyze` permission class covers bridge execution. The safety
  floor is `:needs_confirmation`; Settings Central cannot lower it. Risk tier
  is `high`.
- ADR 0007 requires runtime-facing, effectful operations to enter through
  registered Jido actions. `StockSage.Actions.RunAnalysis` is the registered
  action boundary. `StockSage.TraderBridge` is an internal plugin-owned
  implementation detail behind that boundary.
- ADR 0008 defines durable confirmation requests. `RunAnalysis` creates a
  pending confirmation record when Security Central returns `:needs_confirmation`
  and does not invoke the bridge until approved. The v0.07 `approve_confirmation`
  workflow resumes the action after re-checking policy.
- ADR 0017 defines plugins as contribution packages. All bridge code —
  `StockSage.TraderBridge`, `StockSage.Bridge.Protocol`, `bridge.py`, and
  `StockSage.Actions.RunAnalysis` — lives under `./plugins/stocksage/`. Allbert
  core does not import bridge internals directly.
- ADR 0018 places `StockSage.Actions.RunAnalysis` in the Python bridge
  milestone. `:stocksage_write` authorizes local domain writes only; it does
  not authorize bridge execution. This plan adds `:stocksage_analyze` as the
  distinct execution permission.
- ADR 0019 governs intent engine candidates. When `active_app: :stocksage`,
  RunAnalysis is a candidate scored through the standard engine; no new
  StockSage-specific core predicates are added.
- ADR 0020 (proposed in M1) defines the bridge technology, JSON-over-stdio
  protocol envelope, plugin-ownership boundary, `:stocksage_analyze` permission
  class, and the v0.28 (formerly v0.26) handoff for market-data API call
  hardening.

No existing ADR needs revision for v0.22 if implementation follows ADR 0018
and ADR 0020. ADR 0018 already anticipates `RunAnalysis` in this milestone.

## Vision And Roadmap Alignment

The vision says the Python bridge gets useful analysis into Allbert early;
native Jido trading agents become default only after parity tests prove they
are close enough. v0.22 implements that posture: a supervised bridge with the
same permission, confirmation, and persistence contract that v0.25 (formerly
v0.23) will reuse rather than replace.

Roadmap dependency order is intact:

- v0.17 supplied the plugin contract.
- v0.18 supplied the app/surface contract.
- v0.19 supplied the intent engine with `active_app: :stocksage` candidate
  scoring.
- v0.20 supplied the StockSage plugin, app, domain tables, queue, and safe
  local actions.
- v0.21 supplied memory review and retrieval; the compiled memory index is
  available as optional analysis context when `memory.index_enabled` is true.
- v0.22 adds the Python bridge and first real analysis execution.
- v0.23 (Jido State-Machine Convergence) converts `Confirmations.Store` and
  `Jobs.Scheduler` to Jido.Agents without changing the v0.22 confirmation
  flow.
- v0.24 (Objective Runtime Foundation) adds `objective_id`/`step_id`
  threading through `RunAnalysis` without changing the action contract.
- v0.25 (formerly v0.23) adds native Jido trading agents using the same
  RunAnalysis action and `:stocksage_analyze` confirmation path.
- v0.27 (formerly v0.25) adds StockSage LiveViews that render results from
  the `stocksage_*` tables established in v0.20 and populated in v0.22.
- v0.28 (formerly v0.26) security hardening adds individual Resource Access
  Security Posture governance for market-data API calls made by
  TradingAgents inside the bridge.

v0.22 must not pull later work forward. It does not add StockSage LiveViews,
native Jido agents, market-data API call per-source confirmations, PostgreSQL,
Oban, canvas components, or workspace shell upgrades.

## Expected Inputs

- `StockSage.Plugin`, `StockSage.App`, `StockSage.Supervisor`,
  `StockSage.Plugin.child_spec/1`, and `StockSage.Plugin.actions/0` exist and
  are implemented.
- `stocksage_analyses`, `stocksage_analysis_details`, `stocksage_analysis_queue`,
  and `stocksage_queue_runs` Ecto schemas and migrations are in place.
- `StockSage.Actions.ListAnalyses`, `StockSage.Actions.ShowAnalysis`,
  `StockSage.Actions.QueueAnalysis` are registered and tested.
- `:stocksage_write` is registered as a Security Central permission class with
  its existing scope (local domain writes).
- The v0.07 confirmation workflow is available through `approve_confirmation`,
  `deny_confirmation`, and `mix allbert.confirmations`.
- `AllbertAssist.Actions.Runner.run/3` handles `:needs_confirmation` responses
  from any registered action.
- `AllbertAssist.Repo` and the shared SQLite database are available to
  StockSage plugin code.
- The v0.21 memory index is available; `memory.index_enabled` setting is
  readable from StockSage actions when they want to include memory context.
- Python 3 is available in the operator's environment; `python3` resolves to
  a Python 3.8+ interpreter by default.

## Bridge Technology And Protocol

The bridge uses a JSON-over-stdio Erlang Port. See ADR 0020 for the full
technology choice rationale.

### Protocol Envelope

Request (newline-delimited JSON object):

```
{"id": "<uuid>", "action": "run_analysis", "ticker": "AAPL", "analysis_date": "2026-05-01", "engine": "tradingagents"}
```

Response on success:

```
{"id": "<uuid>", "status": "ok", "result": { <analysis fields> }}
```

Response on error:

```
{"id": "<uuid>", "status": "error", "reason": "<bounded string, max 500 chars>"}
```

Ping/pong for health check:

```
{"id": "<uuid>", "action": "ping"}
{"id": "<uuid>", "status": "ok", "result": "pong"}
```

Rules enforced by `StockSage.Bridge.Protocol`:

- `id` is always required and echoed back unchanged.
- `action` controls dispatch; unknown actions return an error response.
- Missing required params return an error without dispatching to TradingAgents.
- The protocol rejects any request that lacks a recognized `action` field.
- Response bodies from TradingAgents are bounded to `stocksage.bridge_max_output_bytes`
  before serialization; values exceeding the limit are truncated and the
  response includes `"truncated": true`.

### Python Bridge Script

Location: `./plugins/stocksage/priv/python/bridge.py`

Contract:

- Reads newline-delimited JSON from `stdin`.
- Writes newline-delimited JSON to `stdout` (flushed immediately).
- `action: "ping"` responds `{id, status: "ok", result: "pong"}`.
- `action: "run_analysis"` dispatches to TradingAgents, returns result or error.
- Unknown or malformed input returns a bounded error response; does not crash.
- All output to `stderr` is diagnostic only; it does not carry protocol data.
- Does not accept shell strings, file paths, module names, or arbitrary code.
- Does not write to any file path other than through TradingAgents' standard
  behavior; it does not write to Allbert Home or any Elixir-managed path.

## Module Shapes

All modules live under `./plugins/stocksage/` — none in `apps/allbert_assist/`
or `apps/allbert_assist_web/`. Allbert core does not import bridge internals.

```
StockSage.Bridge.Protocol              # JSON encode/decode for protocol envelope
StockSage.TraderBridge                 # GenServer owning the Erlang Port
StockSage.Actions.RunAnalysis          # registered Jido action for bridge execution
```

### `StockSage.Bridge.Protocol`

Location: `./plugins/stocksage/lib/stocksage/bridge/protocol.ex`

```elixir
@spec encode_request(map()) :: {:ok, binary()} | {:error, term()}
@spec decode_response(binary()) :: {:ok, map()} | {:error, term()}
@spec valid_action?(String.t()) :: boolean()
```

`encode_request/1` validates required fields and returns a newline-terminated
JSON binary. `decode_response/1` parses a JSON line and returns a map with
at least `id` and `status` fields. `valid_action?/1` returns true for `"ping"`
and `"run_analysis"`.

### `StockSage.TraderBridge`

Location: `./plugins/stocksage/lib/stocksage/trader_bridge.ex`

A GenServer that wraps a long-lived Erlang Port process.

```elixir
@spec start_link(keyword()) :: GenServer.on_start()
@spec analyze(map()) :: {:ok, map()} | {:error, term()}
@spec ping() :: :ok | {:error, term()}
@spec bridge_status() :: :running | :disabled | :crashed
```

Startup behavior:

- Reads `stocksage.bridge_enabled` from Settings Central at startup.
- When `bridge_enabled` is false, the GenServer starts but does not open a
  Port; `analyze/1` returns `{:error, :bridge_disabled}`.
- When `bridge_enabled` is true, opens a Port to `bridge.py` using
  `Port.open/2` with `{:spawn_executable, python_path}`, args pointing to
  the bridge script resolved from `:code.priv_dir(:stocksage)`.
- `python_path` is resolved from the `stocksage.python_path` setting;
  default `"python3"`.

Request handling:

- `analyze/1` accepts a validated map `{ticker, analysis_date, engine}`,
  generates a UUID request id, encodes the request via `Protocol.encode_request/1`,
  sends the binary to the Port, registers a pending entry keyed by request id,
  and awaits the response with a timeout of `stocksage.bridge_timeout_ms`
  (default 300_000 ms).
- Responses arrive as `{port, {:data, {:eol, line}}}` messages. The GenServer
  decodes the line, matches by id, and replies to the waiting caller.
- Timeout: the pending entry is cleared; the caller receives `{:error, :timeout}`.

Port crash handling:

- On `{:EXIT, port, reason}`: all pending callers receive `{:error, :bridge_crashed}`.
  The port reference is cleared. The next call to `analyze/1` attempts to reopen
  the Port.
- The crash does not propagate to Allbert core supervision.

### `StockSage.Actions.RunAnalysis`

Location: `./plugins/stocksage/lib/stocksage/actions/run_analysis.ex`

```elixir
use Jido.Action,
  name: "run_analysis",
  description: "Run a StockSage analysis for a ticker through the Python bridge.",
  schema: [
    ticker:         [type: :string, required: true],
    analysis_date:  [type: :string, required: true],  # ISO 8601 date string
    engine:         [type: :string, default: "tradingagents"],
    user_id:        [type: :string, required: true],
    queue_entry_id: [type: :string, required: false]
  ]
```

Action flow:

1. Validate schema. Invalid ticker format or unparseable date returns an error
   before Security Central evaluation.
2. Evaluate `:stocksage_analyze` through Security Central.
3. When `:needs_confirmation` (the default):
   - Build a confirmation record with ticker, analysis_date, engine, user_id,
     selected action, permission, risk tier `high`, and a disclosure note that
     TradingAgents will make external market-data API calls.
   - Create the durable pending confirmation record through the v0.07 workflow.
   - Return `:needs_confirmation` with confirmation id and bounded display summary.
4. When the confirmation is approved (`approve_confirmation` resume path):
   - Re-check Security Central with confirmation context.
   - Call `StockSage.TraderBridge.analyze/1` with `{ticker, analysis_date, engine}`.
   - On bridge success: persist the result (see Result Persistence).
   - On bridge error: persist a failed analysis record; return structured error.
   - If a `queue_entry_id` was provided, mark the queue entry completed or failed.
5. Return a structured result map with `analysis_id`, `status`, `ticker`,
   `analysis_date`, `engine`, and a bounded `summary` string.

Registration: `StockSage.Plugin.actions/0` returns the `RunAnalysis` capability
metadata struct. The action is listed in `allbert_plugin.json` under
`contributions.actions`.

### Result Persistence

On a successful bridge response, the action writes:

- `stocksage_analyses` row: `{id, user_id, ticker, analysis_date, engine,
  status: "completed", summary: bounded(result.summary, 500), thread_id,
  session_id, queue_entry_id, trace_id, request_id, bridge_duration_ms,
  inserted_at}`.
- `stocksage_analysis_details` row: `{id, analysis_id, raw_result:
  bounded(result.raw, max_output_bytes), truncated: result.truncated,
  inserted_at}`.

On a failed bridge response, the action writes:

- `stocksage_analyses` row: `{status: "failed", summary: bounded(reason, 200),
  ...}`.
- No `stocksage_analysis_details` row.

On bridge disabled or crashed (before confirmation approval), no row is written.

Queue integration: when `queue_entry_id` is provided, the action updates the
matching `stocksage_analysis_queue` row's status to `"completed"` or `"failed"`
and writes a `stocksage_queue_runs` row recording `{queue_entry_id,
analysis_id, status, started_at, finished_at}`.

## Settings Central Schema

All new settings are added to the StockSage plugin's Settings Central schema
contribution, not to the Allbert core settings namespace.

New keys under `stocksage.*`:

- `stocksage.bridge_enabled` — boolean, default `true`, writable. When false,
  `StockSage.TraderBridge` starts but does not open a Port; `RunAnalysis`
  returns `{:error, :bridge_disabled}` before confirmation.
- `stocksage.python_path` — string, default `"python3"`, writable. Path or
  executable name for the Python 3 interpreter. Used by `TraderBridge` when
  opening the Port.
- `stocksage.bridge_timeout_ms` — integer, default `300_000` (5 min), writable.
  Per-request timeout; exceeding it returns `{:error, :timeout}`.
- `stocksage.bridge_max_output_bytes` — integer, default `1_048_576` (1 MiB),
  writable. Bridge response bodies exceeding this are truncated and marked
  `truncated: true` before persistence.
- `stocksage.analysis_engine` — enum `["tradingagents"]`, default
  `"tradingagents"`, writable. The default engine passed to `RunAnalysis` when
  no `engine` param is supplied. `"tradingagents"` is the only valid value in
  v0.22; v0.25 (formerly v0.23) adds `"native"`.

Existing `permissions.stocksage_analyze` key (added in M1):

- Settings Central key: `permissions.stocksage_analyze`
- Default: `needs_confirmation`
- Safety floor: `needs_confirmation` (Security Central enforces)
- Risk tier: `high`
- Writable: true (but only tightenable; floor prevents setting to `allowed`)

## Signals And Trace Metadata

`allbert.stocksage.analysis_requested` — emitted by `RunAnalysis` when a
confirmation record is created. Includes: confirmation id, ticker, analysis_date,
engine, user_id (no raw analysis content).

`allbert.stocksage.analysis_completed` — emitted on successful bridge return
and persistence. Includes: analysis_id, ticker, analysis_date, engine,
bridge_duration_ms, truncated flag. Does not include raw result body.

`allbert.stocksage.analysis_failed` — emitted on bridge error, crash, or
timeout. Includes: ticker, analysis_date, error class, bounded reason. Does
not include partial TradingAgents output.

Trace sections added in v0.22:

- `## StockSage Analysis` — appears in traces for `RunAnalysis` runs. Includes:
  analysis_id or confirmation id, ticker, analysis_date, engine, bridge status
  (`running` / `disabled` / `crashed`), confirmation status, result summary
  (bounded to 200 chars), and truncated flag. Raw result body is never included.

Existing trace sections that pick up RunAnalysis data:

- `## Action Metadata` — includes `StockSage.Actions.RunAnalysis` as the
  selected action with permission `:stocksage_analyze`.
- `## Confirmation Metadata` — includes the RunAnalysis confirmation record
  when `:needs_confirmation` is returned, per the v0.07 confirmation workflow.

## Milestones

### Milestone 1: ADR 0020, Permission Class, Settings Schema, And bridge.py Scaffold

Goal: accept ADR 0020, register `:stocksage_analyze`, add StockSage settings
schema entries, add `StockSage.Bridge.Protocol`, and add the `bridge.py`
scaffold. No bridge Port process yet.

Implementation tasks:

- Accept ADR 0020 (change status from Proposed to Accepted).
- Add `:stocksage_analyze` to the StockSage plugin's Security Central
  permission contributions. Register it with default `needs_confirmation`,
  safety floor `needs_confirmation`, and risk tier `high`. The permission must
  be visible through `mix allbert.settings get permissions.stocksage_analyze`.
- Add the five new `stocksage.*` settings schema entries to the StockSage
  plugin's settings schema contribution (see Settings Central Schema section).
- Add `StockSage.Bridge.Protocol` with `encode_request/1`, `decode_response/1`,
  and `valid_action?/1`. Encode validates required fields. Decode returns
  `{:ok, map}` or `{:error, reason}`. Unknown actions fail `valid_action?/1`.
- Add `./plugins/stocksage/priv/python/bridge.py` scaffold:
  - Reads newline-delimited JSON from stdin.
  - Dispatches `action: "ping"` → `{id, status: "ok", result: "pong"}`.
  - Dispatches `action: "run_analysis"` → calls TradingAgents (stub/placeholder
    until M2 wires the real call); returns a bounded structured result.
  - Unknown actions → error response.
  - Malformed JSON → bounded error response; does not crash.
  - Flushes stdout after every response.
- Update `StockSage.Plugin.actions/0` to include `RunAnalysis` capability
  metadata (even though the module does not yet exist, the capability metadata
  struct can be inert). Update `allbert_plugin.json` accordingly.
- Add or update the `run-analysis` StockSage skill `SKILL.md` to declare the
  `run_analysis` action and the `:stocksage_analyze` permission class.

Tests:

- `StockSage.Bridge.ProtocolTest`:
  - `encode_request/1` encodes valid maps to newline-terminated JSON.
  - `encode_request/1` returns error for missing `id` or `action` fields.
  - `decode_response/1` parses a valid JSON line.
  - `decode_response/1` returns error for non-JSON input.
  - `valid_action?/1` returns true for `"ping"` and `"run_analysis"`.
  - `valid_action?/1` returns false for unknown actions.
- Settings schema tests: new `stocksage.*` keys are valid; malformed values
  are rejected; `permissions.stocksage_analyze` resolves to `needs_confirmation`.
- `mix allbert.plugins show stocksage` lists `RunAnalysis` in contributions.
- `mix allbert.settings get permissions.stocksage_analyze` returns
  `needs_confirmation`.

Docs:

- ADR 0020 status updated to Accepted.
- Update `docs/plans/v0.22-request-flow.md` Milestone Update Log: M1 section.

Closeout: implemented 2026-05-15. ADR 0020 accepted; `:stocksage_analyze`
registered in core Policy/Risk modules; settings schema entries
(`permissions.stocksage_analyze` core, five `stocksage.*` plugin-contributed);
`StockSage.Bridge.Protocol` and `bridge.py` scaffold; inert `RunAnalysis`
stub registered through `StockSage.Plugin.actions/0`; `run-analysis` skill
created. Protocol/settings/permission/intent tests pass and existing
StockSage suites stay green.

### Milestone 2: StockSage.TraderBridge Supervised Port Process

Goal: the bridge process starts under `StockSage.Supervisor`, responds to
pings, handles port crashes gracefully, and respects `bridge_enabled`.

Implementation tasks:

- Add `StockSage.TraderBridge` GenServer under
  `./plugins/stocksage/lib/stocksage/trader_bridge.ex` (see Module Shapes).
- Add `StockSage.TraderBridge` to `StockSage.Supervisor` child spec. The
  GenServer name is `StockSage.TraderBridge`.
- Implement `bridge_status/0` returning `:running`, `:disabled`, or `:crashed`.
- Implement `ping/0`: sends a ping request through the Port, awaits pong with
  `bridge_timeout_ms`. Returns `:ok` on pong or `{:error, reason}` on timeout
  or crash.
- Implement `analyze/1` with pending caller map, Port send, response routing,
  and timeout handling (see Module Shapes for full protocol).
- Complete `bridge.py` to wire the `run_analysis` action to TradingAgents.
  Validate `ticker` (non-empty string) and `analysis_date` (ISO 8601 date)
  before dispatch; return bounded error for invalid params. Catch all exceptions
  from TradingAgents and return bounded error responses; do not let exceptions
  crash the bridge process.
- Handle Port crash: return `{:error, :bridge_crashed}` to all pending callers;
  clear the Port reference; attempt lazy restart on next `analyze/1` call.

Tests:

- `StockSage.TraderBridgeTest` (integration, requires Python 3 and bridge.py):
  - `ping/0` returns `:ok` when bridge is running.
  - `analyze/1` with valid params returns `{:ok, map}` with result fields.
  - `analyze/1` with invalid ticker/date returns `{:error, :invalid_params}` or
    a bridge error response without hanging.
  - `bridge_enabled: false` causes `bridge_status/0` to return `:disabled` and
    `analyze/1` to return `{:error, :bridge_disabled}` without opening a Port.
  - Port crash simulation: `analyze/1` returns `{:error, :bridge_crashed}` for
    in-flight requests; subsequent call recovers.
- OTP supervision test: `StockSage.TraderBridge` starts as a child of
  `StockSage.Supervisor`; stopping the plugin supervisor stops the bridge;
  bridge crash does not propagate to Allbert core supervision.
- Unit tests for `StockSage.Bridge.Protocol` can run without Python.

Docs:

- Update `docs/plans/v0.22-request-flow.md` Milestone Update Log: M2 section.

Closeout: implemented 2026-05-15. `StockSage.TraderBridge` GenServer owns
the Port to `bridge.py`, runs under `StockSage.Supervisor`, traps exits to
isolate crashes, lazily reopens after a crash, and honors `bridge_enabled`,
`bridge_timeout_ms`, `bridge_max_output_bytes`, and `python_path` settings.
`StockSage.TraderBridgeTest` (tagged `:bridge`) covers ping, valid/invalid
analyze, disabled mode, and port crash recovery; `SupervisorTest` confirms
the bridge boots under the plugin supervisor at runtime startup.

### Milestone 3: RunAnalysis Action, Confirmation Workflow, And Result Persistence

Goal: `StockSage.Actions.RunAnalysis` is a registered, Security
Central-gated Jido action that creates confirmation records, resumes through
the v0.07 approval workflow, calls the bridge, and persists results.

Implementation tasks:

- Add `StockSage.Actions.RunAnalysis` under
  `./plugins/stocksage/lib/stocksage/actions/run_analysis.ex` (see Module
  Shapes).
- Register `RunAnalysis` in `StockSage.Plugin.actions/0` (replace the inert
  capability metadata stub from M1 with the real module reference).
- Add validation for `ticker` format (non-empty, no shell metacharacters,
  max 10 chars) and `analysis_date` (parseable ISO 8601 date, not in the
  far future). Return schema errors before Security Central evaluation.
- Wire Security Central `:stocksage_analyze` check:
  - `:needs_confirmation` path: build a confirmation record including ticker,
    analysis_date, engine, a bounded description of TradingAgents external
    calls as the disclosure note, user_id, risk tier `high`, and the action's
    runner/signal context. Create the pending confirmation. Return the
    confirmation id in the action response.
  - `approved` resume path (invoked by `approve_confirmation`): re-check
    Security Central; call `StockSage.TraderBridge.analyze/1`; handle errors;
    persist result per Result Persistence section; return structured result.
- Wire result persistence: `AllbertAssist.Repo.insert!/1` writes the analysis
  rows after a successful bridge call. Failed bridge calls write a failure row.
- Wire queue integration: when `queue_entry_id` is set, update the queue entry
  status and write a `stocksage_queue_runs` row.
- Update the `run-analysis` skill `SKILL.md` with examples:
  `"analyze AAPL for 2026-05-01"`, `"run StockSage analysis for TSLA"`,
  `"queue analysis for MSFT"`. Include the `run_analysis` action name and
  `:stocksage_analyze` permission in the capability contract section.

Tests:

- `StockSage.Actions.RunAnalysisTest`:
  - Valid params with default policy: action returns `:needs_confirmation`;
    a durable pending confirmation record is created.
  - Confirmation record includes ticker, analysis_date, engine, user_id,
    risk tier `high`, and disclosure note about external calls; does not
    include raw API keys or credentials.
  - Approved resume: Security Central re-checked; bridge is called with
    correct params; result rows are written; action returns structured result.
  - Bridge error on resume: failure row is written; action returns structured
    error; no partial data is leaked.
  - Invalid ticker (special chars, >10 chars) returns schema error before
    Security Central; no confirmation record is created.
  - Invalid analysis_date returns schema error.
  - `queue_entry_id` provided and analysis completes: queue entry is marked
    `completed`; `stocksage_queue_runs` row is written.
  - Cross-user: analysis row carries the requesting `user_id`; list actions
    filter by user_id.
  - `bridge_enabled: false`: action returns `{:error, :bridge_disabled}`
    before creating a confirmation record.
- Confirmation workflow integration: `RunAnalysis` confirmation goes through
  the existing `approve_confirmation` and `deny_confirmation` actions without
  modification.
- Skill validation: `run-analysis` SKILL.md validates against `RunAnalysis`
  capability metadata.

Docs:

- Update `docs/plans/v0.22-request-flow.md` Milestone Update Log: M3 section.

Closeout: implemented 2026-05-15. `StockSage.Actions.RunAnalysis` enforces
schema validation before Security Central, creates durable confirmation
records (with TradingAgents disclosure), executes the bridge on approved
resume through `AllbertAssist.Actions.Confirmations.ApproveConfirmation`,
persists `stocksage_analyses` + `stocksage_analysis_details` on success and
failure rows on bridge errors, and updates queue + queue_runs when a
`queue_entry_id` is provided. The action is marked `resumable?: true` so
the v0.07 approve workflow can route to it; a new `resume_run_analysis`
arm was added to the core approval dispatcher.

### Milestone 4: CLI Surface, Queue Consumption, And Intent Routing

Goal: operators can invoke analysis from the CLI, queue-driven analyses link
to existing queue entries, and natural language "analyze AAPL" routes to
`RunAnalysis` when `active_app: :stocksage`.

Implementation tasks:

- Add `mix stocksage.analyze TICKER DATE` Mix task under
  `./plugins/stocksage/lib/mix/tasks/stocksage/analyze.ex`:
  - Positional args: `ticker` and `analysis_date`.
  - Options: `--engine tradingagents` (default), `--user local` (default),
    `--queue-id <queue_entry_id>` (optional).
  - Calls `StockSage.Actions.RunAnalysis` through
    `AllbertAssist.Actions.Runner.run/3`.
  - When action returns `:needs_confirmation`: displays the confirmation id
    and instructs the user to run
    `mix allbert.confirmations approve <id> --reason "..."`.
  - When action returns a result (on confirmed resume): displays analysis id,
    ticker, date, engine, and a bounded summary.
  - Invalid args or missing ticker/date: clear error message; exit non-zero.
- Queue consumption from CLI: `--queue-id <id>` links the analysis to a
  specific `stocksage_analysis_queue` row. The CLI resolves the queue entry
  and passes `queue_entry_id` to `RunAnalysis`. If the queue entry is already
  consumed, the CLI displays a clear message.
- Intent routing: ensure the `run-analysis` skill examples from M3 are
  discoverable through the v0.19 engine when `active_app: :stocksage`. The
  engine's existing candidate scoring handles this; no new StockSage-specific
  core predicates are added. Verify that `mix allbert.ask --session-id <id>
  "analyze AAPL for 2026-05-01"` routes to `RunAnalysis` as the top candidate
  when `active_app: :stocksage` is set in the session scratchpad.
- Verify that `StockSage.Actions.ListAnalyses` and `StockSage.Actions.ShowAnalysis`
  already return bridge-run analyses from the `stocksage_analyses` table
  (they query the table by user_id; no schema change needed).

Tests:

- `Mix.Tasks.Stocksage.AnalyzeTest`:
  - Valid ticker + date + default options: calls `RunAnalysis` through the
    runner; prints confirmation id.
  - `--queue-id <id>`: passes `queue_entry_id` to the action; after approval,
    queue entry status is `completed`.
  - Missing ticker or date: prints clear error and exits non-zero.
  - Invalid ticker chars: action rejects before confirmation; CLI prints error.
- Intent routing test: with a session scratchpad entry `active_app: :stocksage`,
  `Engine.collect_candidates/1` for `"analyze AAPL"` includes `RunAnalysis`
  as a scored candidate.
- `StockSage.Actions.ListAnalysesTest`: bridge-run analyses appear in list
  results for the correct user; analyses from another user do not appear.

Docs:

- Update `docs/plans/v0.22-request-flow.md` Milestone Update Log: M4 section.

Closeout: implemented 2026-05-15. `mix stocksage.analyze` Mix task wires
the CLI surface, including `--queue-id` plumbing into RunAnalysis.
`StockSage.ActionsTest` extends to verify `run_analysis` is a candidate
when `active_app: :stocksage` (no new core predicate). End-to-end
operator smoke confirmed: CLI → confirmation id → approve → persisted
`stocksage_analyses` + `stocksage_analysis_details` rows.

### Milestone 5: Traces, Release Docs, Release Gate, And User Testing

Goal: analysis runs are fully traceable, release documentation is complete,
and the operator smoke proves the end-to-end user loop works.

Implementation tasks:

- Add `## StockSage Analysis` trace section to `AllbertAssist.Trace` for
  `RunAnalysis` turns. The section must include analysis_id or confirmation id,
  ticker, analysis_date, engine, bridge_status, confirmation status, bounded
  result summary (max 200 chars), and truncated flag. Raw result body, raw
  TradingAgents output, and API keys must never appear in the trace.
- Add redaction tests: assert no raw result body, no raw API response, and no
  secrets appear in trace files or CLI output generated during smoke.
- Register signals `allbert.stocksage.analysis_requested`,
  `allbert.stocksage.analysis_completed`, and
  `allbert.stocksage.analysis_failed` in the StockSage signal documentation
  (not a new signal bus wiring; these are emitted through the existing action
  runner signal path).
- Ensure `mix allbert.plugins show stocksage` lists `RunAnalysis` in
  `contributions.actions` with correct permission and capability metadata.
- Update `CHANGELOG.md`, root `README.md`, `apps/allbert_assist/README.md`,
  `docs/plans/roadmap.md`, `AGENTS.md`, and `DEVELOPMENT.md` per the Release
  Documentation Closeout section.
- Bump `apps/allbert_assist/mix.exs` and `apps/allbert_assist_web/mix.exs`
  to `0.22.0`.
- Run the full warning gate.
- Complete the Operator Smoke for Implementation (see below).

Tests:

- Trace redaction: no raw analysis body, no TradingAgents raw output, and no
  credentials appear in trace files produced during a smoke run.
- End-to-end: `mix stocksage.analyze AAPL 2026-05-01 --trace` → confirmation
  created → `mix allbert.confirmations approve <id>` → trace includes
  `## StockSage Analysis` with bounded summary.
- `mix allbert.plugins show stocksage` lists `RunAnalysis` with
  `permission: :stocksage_analyze`, `confirmation: required`.

Docs:

- Complete Release Documentation Closeout checklist.
- Update `docs/plans/v0.22-request-flow.md` Milestone Update Log: M5 section.

Closeout: implemented 2026-05-15. `## StockSage Analysis` trace section added
with bounded metadata; `StockSage.TraceTest` covers rendering, the `none`
path, redaction, and the 200-char summary bound. Release docs updated
(CHANGELOG, README, apps/allbert_assist/README, AGENTS, DEVELOPMENT,
roadmap). Versions bumped to `0.22.0` across `mix.exs`,
`apps/allbert_assist/mix.exs`, and `apps/allbert_assist_web/mix.exs`.
Release tag `v0.22` is pending operator acceptance.

## Test Plan

Focused test files required before M5 closeout:

- `StockSage.Bridge.ProtocolTest`
- `StockSage.TraderBridgeTest` (integration; tagged `:integration` or `:bridge`)
- `StockSage.Actions.RunAnalysisTest`
- `Mix.Tasks.Stocksage.AnalyzeTest`
- `StockSage.Actions.ListAnalysesTest` (extended for bridge-run results)
- `AllbertAssist.Intent.EngineTest` (extended for RunAnalysis candidate with `active_app: :stocksage`)
- `AllbertAssist.TraceTest` (extended for StockSage Analysis trace section and redaction)

Coverage requirements:

- Protocol encode/decode round-trips; unknown actions and malformed JSON are
  handled without crash.
- Bridge process starts, pings, analyzes, handles crash, and respects
  `bridge_enabled: false`.
- `RunAnalysis` creates a confirmation record on first call; resume calls the
  bridge and persists results; failures produce failure rows without leaking
  partial data.
- Schema validation rejects malformed tickers, invalid dates, and missing
  required params before any confirmation record is created.
- `queue_entry_id` integration marks queue entries completed/failed after run.
- CLI `mix stocksage.analyze` produces a confirmation id on first call; accepts
  `--queue-id`; rejects missing args.
- Intent engine returns `RunAnalysis` as a candidate when `active_app: :stocksage`.
- Trace section is present and redacted for all run outcomes.
- All tests use a temporary ALLBERT_HOME; none write to a real `~/.allbert`.
- Bridge integration tests are tagged so they can be skipped in CI environments
  without Python.

Full gate (run after every milestone and required before M5 closeout):

```sh
mix test --exclude integration          # unit gate
mix test --include integration          # full gate when Python is available
mix format --check-formatted
mix compile --warnings-as-errors
mix credo --strict
mix dialyzer
mix precommit
```

## Operator Smoke For Implementation

Use a disposable Allbert Home for all smoke steps. Python 3 and TradingAgents
must be installed in the operator's environment.

```sh
export SMOKE_HOME=$(mktemp -d /tmp/allbert-v022-smoke.XXXXXX)
```

### M1 Smoke: Settings And Plugin Contributions

```sh
ALLBERT_HOME="$SMOKE_HOME" mix allbert.plugins show stocksage
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings get permissions.stocksage_analyze
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings get stocksage.bridge_enabled
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings get stocksage.bridge_timeout_ms
```

Expected: plugin show lists `RunAnalysis` in contributions with
`permission: :stocksage_analyze`. The settings return their defaults
(`needs_confirmation`, `true`, `300000`).

### M2 Smoke: Bridge Startup And Health

```sh
# Allbert should auto-start the bridge when StockSage plugin is loaded.
# Verify via plugin inspection or a direct ping task if one is added.
ALLBERT_HOME="$SMOKE_HOME" mix allbert.plugins show stocksage
# If a diagnostic bridge ping command is added, run it here.
```

Expected: plugin shows bridge status `running`. No crash or startup error
in Allbert logs.

### M3/M4 Smoke: End-To-End Analysis Request

```sh
# Request analysis — should create a confirmation record
ALLBERT_HOME="$SMOKE_HOME" mix stocksage.analyze AAPL 2026-05-01 --user local

# The command displays: "Confirmation required. ID: <confirmation-id>"
# List the pending confirmation
ALLBERT_HOME="$SMOKE_HOME" mix allbert.confirmations list

# Approve it
ALLBERT_HOME="$SMOKE_HOME" mix allbert.confirmations approve <confirmation-id> --reason "smoke test"

# Verify result persisted — the approve command should display the analysis summary.
# Also verify via list:
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask "list my stocksage analyses" \
  # (or with explicit StockSage session context if the ask command supports it)
```

Expected: analysis is persisted in `stocksage_analyses`. The result includes
ticker `AAPL`, status `completed`, and a non-empty bounded summary.

### M4 Smoke: Queue-Driven Analysis

```sh
# Queue an analysis entry via the existing queue action
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask "queue analysis for TSLA for 2026-05-01"

# List queue entries and note the queue_entry_id
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask "list stocksage queue"

# Run analysis tied to the queue entry
ALLBERT_HOME="$SMOKE_HOME" mix stocksage.analyze TSLA 2026-05-01 \
  --queue-id <queue_entry_id> --user local

# Approve
ALLBERT_HOME="$SMOKE_HOME" mix allbert.confirmations list
ALLBERT_HOME="$SMOKE_HOME" mix allbert.confirmations approve <id> --reason "smoke test"

# Verify queue entry is marked completed
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask "show stocksage queue"
```

Expected: queue entry status is `completed` after approval. A
`stocksage_queue_runs` row is written linking the queue entry to the analysis.

### M4 Smoke: Intent Routing With Active App Context

```sh
# Set active_app to stocksage in a named session
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --session-id ss1 \
  "set active app to stocksage"

# Ask for analysis in the same session
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --session-id ss1 --trace \
  "analyze MSFT for 2026-05-14"
```

Expected: the trace shows `RunAnalysis` as the top candidate with
`source: skill` and `app: stocksage`. A confirmation is created.

### M5 Smoke: Trace Redaction

```sh
ALLBERT_HOME="$SMOKE_HOME" mix stocksage.analyze AAPL 2026-05-01 \
  --trace --user local
ALLBERT_HOME="$SMOKE_HOME" mix allbert.confirmations approve <id> --reason "smoke trace test"
# Inspect trace file
cat "$SMOKE_HOME/memory/traces/*.md" | grep -i "api_key\|password\|secret\|token"
```

Expected: zero matches. Trace file includes `## StockSage Analysis` section
with bounded summary. No raw result body appears.

### M5 Smoke: bridge_enabled False

```sh
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings set stocksage.bridge_enabled false
ALLBERT_HOME="$SMOKE_HOME" mix stocksage.analyze AAPL 2026-05-01 --user local
```

Expected: action returns `{:error, :bridge_disabled}` before creating any
confirmation record. CLI prints a clear disabled message and exits non-zero.

### Cleanup

```sh
rm -rf "$SMOKE_HOME"
```

## Release Documentation Closeout

M5 completes the release documentation pass:

- `CHANGELOG.md` has a `v0.22 - StockSage Python Bridge` section with status,
  added behavior (RunAnalysis, TraderBridge, bridge.py, mix stocksage.analyze,
  `:stocksage_analyze` permission), changed behavior (StockSage plugin actions
  list updated), and safety notes.
- Root `README.md` describes the v0.22 StockSage analysis loop: how to run
  `mix stocksage.analyze`, how to approve the confirmation, and how to see
  results through `mix allbert.ask`.
- `apps/allbert_assist/README.md` notes the StockSage plugin now contributes
  a Python bridge and `RunAnalysis` action; directs readers to
  `./plugins/stocksage/` for bridge and action code.
- `docs/plans/roadmap.md` marks v0.22 implemented and ready for operator
  manual verification.
- `docs/plans/v0.22-request-flow.md` Milestone Update Log records actual module
  names, CLI command output shapes, and any deferred behavior.
- `docs/plans/v0.25-plan.md` (formerly v0.23) is updated to confirm that it
  inherits `:stocksage_analyze` permission and result persistence contracts
  from v0.22; the native agent path replaces only
  `StockSage.TraderBridge.analyze/1` calls.
- `docs/adr/0020-stocksage-python-bridge-protocol.md` is marked Accepted
  (done in M1).
- `AGENTS.md` updated with v0.22 regression context and guidance for StockSage
  workspace app work.
- `DEVELOPMENT.md` updated to treat v0.22 as regression context.
- `apps/allbert_assist/mix.exs` and `apps/allbert_assist_web/mix.exs` bumped
  to `0.22.0`.
- Expected release tag: `v0.22`. Tag after operator acceptance.

Release documentation must preserve the safety story:

- v0.22 adds real analysis execution through a supervised Python bridge.
- Analysis execution requires confirmation by default; the `:stocksage_analyze`
  safety floor is `:needs_confirmation` and cannot be lowered via settings.
- TradingAgents external API calls are included in the operator's approval
  scope; they are not individually confirmed in v0.22. v0.28 (formerly
  v0.26) hardens this.
- Raw analysis output is bounded and never appears in traces, CLI output, or
  signals.
- Bridge crash does not affect Allbert core supervision; pending callers
  receive a bounded error.
- v0.22 does not add StockSage LiveViews, native Jido agents, PostgreSQL, or
  market-data-source-level confirmations.

## Non-Goals

- No Jido.Agent conversion of `Confirmations.Store` or `Jobs.Scheduler`
  (v0.23).
- No objective runtime, objective state, or `objective_id` threading (v0.24).
- No native Jido trading agents as default (v0.25, formerly v0.23).
- No StockSage LiveViews or workspace UI (v0.27, formerly v0.25).
- No per-market-data-source Resource Access Security Posture confirmations
  (v0.28, formerly v0.26, hardening).
- No PostgreSQL or Oban as a dependency.
- No arbitrary shell escape through the Python bridge; the protocol is
  JSON-structured and action-dispatched only.
- No bridge calls from the intent agent without `active_app: :stocksage`
  session evidence.
- No automatic queue consumption on a schedule; queue entries are consumed
  only by explicit operator action or confirmed runtime action.
- No canvas components or ephemeral UI substrate (v0.26/v0.30, formerly
  v0.24/v0.28).
- No StockSage memory namespace registration (v0.29, formerly v0.27).

## Definition Of Done

v0.22 is done when:

- ADR 0020 is accepted and committed.
- `StockSage.Bridge.Protocol` exists with encode, decode, and action-validation
  functions, and passes focused unit tests.
- `StockSage.TraderBridge` starts under `StockSage.Supervisor`, responds to
  pings, routes responses to callers, handles crashes without propagating to
  Allbert core, and respects `bridge_enabled: false`.
- `./plugins/stocksage/priv/python/bridge.py` accepts JSON requests, dispatches
  to TradingAgents, returns bounded JSON responses, and handles errors without
  crashing.
- `StockSage.Actions.RunAnalysis` is a registered Jido action with `:stocksage_analyze`
  permission, creates a durable confirmation record on first call, calls
  the bridge on approved resume, and persists results.
- `mix stocksage.analyze AAPL 2026-05-01` creates a confirmation, and approving
  it through `mix allbert.confirmations approve` produces a persisted analysis
  result visible through `mix allbert.ask "list my stocksage analyses"`.
- `mix allbert.plugins show stocksage` lists `RunAnalysis` with correct
  permission and capability metadata.
- Queue consumption works: `--queue-id` links an analysis run to a queue entry
  and marks it completed/failed.
- Intent routing: with `active_app: :stocksage`, "analyze AAPL" routes to
  `RunAnalysis` as the top candidate through the v0.19 engine.
- Trace output includes `## StockSage Analysis` section with bounded metadata
  and no raw result body.
- Redaction tests confirm no raw analysis body, no API keys, and no partial
  TradingAgents output appear in traces or CLI output.
- The operator smoke from "Operator Smoke For Implementation" passes
  end-to-end, including the intent routing smoke and the `bridge_enabled: false`
  negative check.
- The Release Documentation Closeout checklist is complete.
- `apps/allbert_assist` and `apps/allbert_assist_web` are both at `0.22.0`.
- The full warning gate passes clean.
