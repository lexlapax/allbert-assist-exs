# Allbert v0.19 Cross-Surface Intent Enrichment

Formerly tracked as v0.15, then v0.21, then v0.22 before the roadmap was
resequenced.

## Status

Status: implemented through M6 closeout on 2026-05-15. Ready for operator manual verification.

## Source Documents

- `docs/plans/roadmap.md`
- `docs/plans/allbert-jido-vision.md`
- `docs/plans/v0.11-plan.md`
- `docs/plans/v0.11-request-flow.md`
- `docs/plans/v0.13-plan.md`
- `docs/plans/v0.13-request-flow.md`
- `docs/plans/v0.14-plan.md`
- `docs/plans/v0.14-request-flow.md`
- `docs/plans/v0.15-plan.md`
- `docs/plans/v0.15-request-flow.md`
- `docs/plans/v0.17-plan.md`
- `docs/plans/v0.17-request-flow.md`
- `docs/plans/v0.18-plan.md`
- `docs/plans/v0.18-request-flow.md`
- `docs/adr/0001-signal-first-jido-runtime.md`
- `docs/adr/0006-security-central.md`
- `docs/adr/0007-jido-native-internal-runtime-boundaries.md`
- `docs/adr/0012-resource-access-security-posture.md`
- `docs/adr/0013-uri-first-resource-identity.md`
- `docs/adr/0014-local-workspace-identity.md`
- `docs/adr/0015-allbert-app-contract-and-surface-dsl.md`
- `docs/adr/0016-channel-adapter-boundary-and-identity-mapping.md`
- `docs/adr/0017-allbert-plugin-contract.md`
- `docs/adr/0019-cross-surface-intent-enrichment.md`

## Purpose

v0.19 upgrades Allbert's intent layer from deterministic route predicates into
a registry-aware, app-aware, surface-aware intent engine over the capabilities
that already exist. It keeps the v0.11 `AllbertAssist.Intent.Decision`
contract as the execution gate, but gives the decision richer candidate input:
registered actions, trusted skills, jobs, channels, confirmations, existing
trace and memory metadata, plugin provenance, session scratchpad state,
`active_app`, and v0.18 app/surface metadata.

The result should feel smarter to a user without becoming more dangerous. If a
session is in StockSage, Allbert can prefer StockSage actions, skill paths, and
surfaces. If the same words arrive in neutral Allbert context, StockSage is
only a lower-ranked or suggested candidate unless the request supplies explicit
app evidence. The engine may ask a model for structured classification when
deterministic confidence is low, but model output is treated as untrusted
proposal data and must validate through the same registries and Security
Central posture before any action runs.

## Readiness Decision

v0.19 is ready for implementation when the plan, request-flow doc, roadmap,
development guide, and ADRs agree on these points:

- Runtime turns still enter through `AllbertAssist.Runtime.submit_user_input/1`
  and `AllbertAssist.Agents.IntentAgent.respond/1`.
- `AllbertAssist.Intent.Decision` remains the inert decision contract and the
  only decision shape returned to runtime responses, traces, channels, jobs,
  and conversations.
- Candidate ranking may prioritize by app/session context, but never grants
  permission, trust, or execution authority.
- `active_app` is session context, not authorization. Unknown app ids are
  ignored safely and never atomized from external input.
- Registered app surface metadata can produce a navigation decision, but
  v0.19 does not implement the v0.26 (formerly v0.24) workspace shell,
  generated UI, dynamic routes, or canvas rendering.
- Optional model-assisted classification is bounded by settings, deterministic
  fallback, strict validation, and redacted trace metadata.
- Existing v0.11 behavior for URL/document/package/shell/script/import
  decisions is preserved.
- Existing v0.18 behavior for app/surface validation and `active_app: :allbert`
  defaulting is preserved.
- Reviewed memory retrieval from v0.21 is not a prerequisite. v0.19 consumes
  existing memory and trace metadata only, and exposes a plug-in point for
  v0.21 to add reviewed memory signals later.

## Architecture Readiness Audit

The v0.19 plan is consistent with the current architecture:

- ADR 0001 requires signal-first runtime coordination. v0.19 enriches the
  decision attached to runtime signals and traces; it does not introduce a
  separate intent bus.
- ADR 0006 keeps permission, risk, redaction, trust, and audit decisions in
  Security Central. v0.19 ranking never bypasses those checks.
- ADR 0007 requires runtime-facing, effectful behavior to resolve through
  registered actions. v0.19 may choose an action candidate, but execution still
  happens through `AllbertAssist.Actions.Runner.run/3`.
- ADR 0012 keeps resource access operation classes and remembered grants
  operation-scoped. v0.19 may rank resource workflows, but cannot collapse URL,
  package, skill import, shell, script, or local resource posture into generic
  network or shell intent.
- ADR 0014 defines `user_id`, `thread_id`, `session_id`, and `active_app`
  context. v0.19 reads these fields and records them in decisions without
  adding hosted auth, roles, or new identity tables.
- ADR 0015 defines the app/surface contract. v0.19 consumes app registry and
  `SurfaceProvider` metadata, but does not extend the surface DSL.
- ADR 0017 defines plugins as contribution packages, not authority. v0.19 may
  use plugin ids as explainability metadata only.

Add ADR 0019 for cross-surface intent enrichment as part of M1. The intent
engine introduces a candidate model, registry-gated ranking, and optional model
classification with specific behavioral invariants that must be documented to
protect future implementors. ADR 0019 records: the engine as proposal
infrastructure only; model output as untrusted ranked proposal data that cannot
create candidates or change permissions; registry validation as the mandatory
gate before any candidate may be selected; `active_app` as ranking context
only, never authorization; and the invariant that candidate scores cannot exceed
Security Central permission floors or confirmation requirements. Add a further
new ADR only if a future implementation change crosses one of those boundaries.

## Vision And Roadmap Alignment

The vision says Allbert should listen, remember, route intent, invoke
capabilities safely, and learn from traces while remaining inspectable. v0.19
is the routing upgrade: it makes Allbert better at choosing between existing
skills, actions, channels, jobs, apps, and surfaces without adding new powers.

Roadmap dependency order is intact:

- v0.11 supplied the `Decision`, resource posture, and Approval Handoff shape.
- v0.12 supplied local `user_id` and `thread_id` conversation context.
- v0.13 supplied scheduled jobs as runtime-originated background context.
- v0.14 supplied session scratchpad and `active_app`.
- v0.15 supplied minimal app action/skill registration.
- v0.16 supplied channel adapters and channel metadata.
- v0.17 supplied plugin provenance and plugin-contributed actions, skills,
  settings, channels, and apps.
- v0.18 supplied the full app/surface contract, `SurfaceProvider`, and the
  default `active_app: :allbert` home app.
- v0.19 consumes those inputs and prepares v0.20 StockSage, v0.21 memory
  review, v0.26 (formerly v0.24) workspace UI, and v0.28 (formerly v0.26)
  security evals.

v0.19 must not pull later work forward. It does not build StockSage domain
records, reviewed memory retrieval, Python bridge execution, native trading
agents, workspace shell rendering, LiveViews, security hardening, canvas
integration, or app generation.

## Current State

- `AllbertAssist.Runtime.submit_user_input/1` normalizes request identity,
  thread, session, and `active_app`, emits input/response signals, persists
  conversation messages, records traces, and calls the intent agent.
- `AllbertAssist.Agents.IntentAgent.respond/1` uses deterministic route
  predicates and builds v0.11 `AllbertAssist.Intent.Decision` structs before
  running actions.
- `AllbertAssist.Intent.Decision` validates selected actions through
  `AllbertAssist.Actions.Registry`, validates selected skills through
  `AllbertAssist.Skills`, applies Security Central authorization, normalizes
  candidate action summaries, records identity/session/app fields, and returns
  trace metadata.
- `AllbertAssist.Actions.Registry` exposes core and plugin-contributed action
  capabilities with permission, execution mode, confirmation, app id, and
  plugin provenance metadata.
- `AllbertAssist.Skills.Registry` exposes built-in, project, user, imported,
  plugin, and app skill roots with trust/source metadata.
- `AllbertAssist.App.Registry` exposes registered apps, registered surfaces,
  `SurfaceProvider` surfaces/catalogs, app action/skill metadata, and safe app
  id normalization.
- `AllbertAssist.Session` and the v0.14 scratchpad store volatile
  `{user_id, session_id}` context including `active_app`.
- v0.21 reviewed-memory retrieval does not exist yet. Existing memory input is
  limited to current markdown memory functions, conversation context, traces,
  and action metadata already available before v0.19.

## Expected Inputs

- v0.11 execution-aware intent decisions are implemented and released.
- v0.12 local identity and SQLite conversation history are implemented and
  released.
- v0.13 scheduled jobs are implemented and released.
- v0.14 session scratchpad and `active_app` are implemented and released.
- v0.15 minimal app registration is implemented and released.
- v0.16 channel adapters are implemented and released.
- v0.17 plugin contract is implemented and released.
- v0.18 full app/surface contract and `active_app: :allbert` defaulting are
  implemented and released.

## Guiding Rules

1. Preserve the decision contract. v0.19 may add optional metadata fields to
   `Decision.trace_metadata` and candidate lists, but should not break callers
   that consume the v0.11 decision map.

2. Ranking is not authority. Candidate scores, app affinity, model
   classifications, channel context, plugin provenance, and surface metadata
   never bypass `Actions.Registry`, Security Central, confirmation workflow,
   skill trust rules, resource posture, or action runner lifecycle.

3. Keep deterministic behavior first. The engine must produce useful decisions
   without model assistance. Model classification is optional, setting-gated,
   timeout-bounded, and used only to rank known candidates.

4. Never atomize external ids. App ids, plugin ids, channel ids, action names,
   skill names, job ids, and surface ids from request text, channels, jobs, or
   model output must normalize through existing registries or remain strings in
   rejected diagnostics.

5. App context must be explicit. Prioritize app actions, app skill paths, and
   app surfaces only when request metadata, scratchpad `active_app`, channel
   context, job context, or an already selected route provides explicit
   evidence. Neutral `:allbert` context should not silently route into
   StockSage or another app.

6. Surface navigation is a decision target, not UI generation. A selected
   surface target returns a safe navigation suggestion or action result through
   existing CLI/web/channel output. v0.26 (formerly v0.24) owns the workspace
   shell and ephemeral UI rendering.

7. Existing resource workflows remain operation-scoped. Package installs,
   shell commands, scripts, URL summaries, document inspection, online skill
   import, local skill import, and unsupported MCP/agent URIs keep the v0.11
   resource access and Approval Handoff posture.

8. Preserve channel and job boundaries. Channels and jobs may supply context
   to the engine, but they do not fork intent logic or own separate policy.

9. Trace the why. Selected and rejected candidates must include bounded,
   redacted reasons so the operator can inspect why Allbert picked or refused
   a route.

## Intent Engine Contract

Add a small engine layer under `AllbertAssist.Intent` that the existing intent
agent can call before building or returning a `Decision`:

- `AllbertAssist.Intent.Engine`
- `AllbertAssist.Intent.Candidate`
- `AllbertAssist.Intent.Ranker`
- `AllbertAssist.Intent.Classifier`
- `AllbertAssist.Intent.Evals`

Recommended public API:

```elixir
@spec decide(map()) ::
        {:ok, AllbertAssist.Intent.Decision.t()} | {:error, term()}
```

The input map is the normalized runtime request plus current deterministic
route hints. It should include:

- `:text`
- `:channel`
- `:user_id`
- `:operator_id`
- `:thread_id`
- `:session_id`
- `:active_app`
- `:thread_context`
- `:metadata`
- `:input_signal_id`

The output is still `AllbertAssist.Intent.Decision.t()`.

Implementation may keep the existing route predicates in
`AllbertAssist.Agents.IntentAgent` at first, but the candidate collection,
ranking, validation, and trace metadata should live in the new intent modules
so later milestones do not add more private predicates directly to the agent.

## Candidate Model

Represent ranked candidates as plain maps or a lightweight struct with fields
equivalent to:

- `:kind`: `:action`, `:skill`, `:surface`, `:job`, `:channel`, `:memory`,
  `:refusal`, or `:direct_answer`
- `:id`: string id or action/skill/surface name
- `:label`: bounded display label
- `:source`: `:deterministic`, `:registry`, `:app`, `:plugin`, `:job`,
  `:channel`, `:memory`, `:trace`, or `:model`
- `:score`: float `0.0..1.0`
- `:selected?`: boolean
- `:status`: `:selected`, `:candidate`, `:rejected`, or `:unavailable`
- `:reason`: bounded operator-facing explanation
- `:rejection_reason`: atom or string when rejected
- `:action_name`, `:skill_name`, `:surface_id`, `:job_id`, `:channel_id`
- `:app_id`: atom when known through registry
- `:plugin_id`: string when known through registry
- `:permission`
- `:execution_mode`
- `:confirmation`
- `:resource_access`: list of v0.11 resource posture entries when applicable
- `:trace_metadata`: redacted map

Candidates are proposal data. Before a candidate can become selected, it must
validate through the relevant registry. Unknown actions, untrusted skills,
invalid app ids, disabled channels, blocked jobs, unavailable surfaces, or
permanently denied permissions remain rejected candidates with diagnostics.

## Candidate Sources

The engine should collect candidates from these sources:

- Existing deterministic route hints from `IntentAgent.route/1` and resource
  workflow detection.
- `Actions.Registry` capabilities, including app id and plugin provenance.
- `Skills.Registry` records, including trust, source scope, app skill paths,
  plugin skill roots, and activation status.
- `App.Registry` app identities, legacy navigation surfaces, and
  `SurfaceProvider` surfaces/catalog entries.
- `Session` scratchpad context, especially `active_app`.
- `Conversations` recent thread context from the runtime request.
- Existing markdown memory metadata and recent memory action summaries where
  available before v0.21. Do not add reviewed-memory retrieval yet.
- Confirmation metadata for pending or recently resolved requests where the
  request appears to be approving, denying, or asking about pending work.
- Jobs metadata for requests that ask to run, pause, resume, list, or inspect
  scheduled work.
- Channel metadata for requests that ask to inspect or configure Telegram,
  email, or future registered channels.
- Plugin metadata for explaining why an action, skill, channel, or app exists.

Candidate collection must be bounded. Use caps such as:

- At most 20 action candidates.
- At most 20 skill candidates.
- At most 20 surface candidates.
- At most 10 job candidates.
- At most 10 channel candidates.
- At most 80 total candidates before final ranking.

Caps should be settings-backed where useful, with conservative defaults.

## Ranking Rules

The first implementation should use deterministic scoring with optional model
assistance as an additive signal.

Recommended scoring inputs:

- exact command/route phrase match
- registry action name or skill name match
- resource operation class match
- active app affinity
- app skill path match
- app action capability match
- app surface path/label match
- channel/job keyword match
- recent confirmation/job/channel context
- recent thread context
- plugin provenance match
- model classification confidence, if enabled and valid

Ranking must apply these safety filters before selection:

- selected actions must be registered and exposed to the agent or appropriate
  runtime surface
- selected skills must be trusted or activation-only according to existing
  skill rules
- selected surfaces must come from a registered `SurfaceProvider` or validated
  legacy app surface
- selected app ids must already exist in the app registry
- selected permissions and execution modes must come from action capability
  metadata, not model output
- selected resource access entries must validate through v0.11 resource
  posture modules
- denied permissions produce refusal or confirmation-blocked decisions rather
  than executing

Tie-breaking should prefer:

1. exact deterministic risky-resource route, so safety posture stays stable
2. active app action or skill when explicit `active_app` is not `:allbert`
3. core Allbert action or skill
4. surface navigation suggestion
5. direct answer

When confidence is below the configured threshold, prefer a clarifying direct
answer or refusal over a side-effecting action.

## Optional Model-Assisted Classification

Add a setting-gated classifier path. Defaults should keep deterministic-only
behavior unless the operator has configured a model profile suitable for intent
classification.

Recommended settings:

- `intent.model_assist_enabled`: boolean, default `false`
- `intent.model_profile`: string, default from existing fast/local profile
  when configured
- `intent.model_timeout_ms`: integer, default `3000`
- `intent.model_min_confidence`: float, default `0.72`
- `intent.max_candidates`: integer, default `80`
- `intent.trace_rejected_candidates`: boolean, default `true`

The classifier prompt must receive a bounded candidate summary, not raw
secrets, full memory files, raw traces, or unbounded conversation history.

Classifier output must be strict JSON or map-like data with only:

- `selected_kind`
- `selected_id`
- `confidence`
- `reason`
- `alternatives`

Validation rules:

- `selected_id` must match a collected candidate.
- `selected_kind` must match the candidate kind.
- `confidence` must be numeric `0.0..1.0`.
- Unknown fields are ignored.
- Invalid, timed-out, low-confidence, or non-JSON output becomes a diagnostic
  and deterministic ranking proceeds.

Model output cannot create a candidate, invent an action, select a hidden
action, declare trust, change permissions, or approve confirmation.

## App And Surface Routing

When `active_app` is a known app other than `:allbert`, the engine should:

- prioritize actions whose capability `app_id` matches `active_app`
- prioritize skills whose source scope or app contribution matches
  `active_app`
- prioritize surfaces whose `app_id` matches `active_app`
- include app and surface labels in candidate reasons
- keep all non-matching app candidates available but lower ranked

When `active_app` is `:allbert` or nil, the engine should:

- prefer core Allbert skills and actions
- avoid selecting app-specific actions unless the prompt explicitly names the
  app or a registered app surface/action/skill
- may suggest an app surface as an alternative when the prompt names an app
  but lacks enough context to run an app action

Surface navigation outcomes should be represented as decisions with:

- `intent: :open_surface` or a more specific surface intent
- no executable action unless a registered navigation action already exists
- selected surface metadata in `trace_metadata`
- safe message text such as "Open StockSage at /stocksage"
- no dynamic route creation and no generated UI payload

## Integration Points

### Runtime

`AllbertAssist.Runtime` should continue normalizing request context and calling
the intent agent. v0.19 may add trace metadata fields, but should avoid changing
the public runtime request/response shape except for additive decision
metadata.

Runtime responses should include:

- selected decision map
- selected and rejected candidate summaries
- active app
- app id / surface id when a surface route is selected
- model-assist diagnostics when used
- unchanged `user_id`, `thread_id`, and `session_id`

### Intent Agent

`AllbertAssist.Agents.IntentAgent` should delegate candidate collection and
ranking to the new intent engine while preserving current deterministic routes
and action execution paths.

The agent should not grow another long list of app-specific predicates. App
specificity comes from app/action/skill/surface registry metadata.

### Actions

No new side-effecting action is required. If implementation needs operator
inspection, add read-only actions only:

- `explain_intent`
- `list_intent_candidates`

These actions must be `permission: :read_only`, `exposure: :internal` or
operator-only unless there is a clear agent-facing use, and they must not run
selected candidates.

### Settings

Add settings schema entries under the static core settings schema:

- `intent.model_assist_enabled`: boolean, default `false`
- `intent.model_profile`: string, default `"fast"` or current local fast alias
- `intent.model_timeout_ms`: integer, default `3000`
- `intent.model_min_confidence`: number, default `0.72`
- `intent.max_candidates`: integer, default `80`
- `intent.trace_rejected_candidates`: boolean, default `true`

Settings values must validate through Settings Central and appear redacted
where appropriate. None of these settings grant execution authority.

### Traces And Signals

Runtime traces should add an `Intent Candidates` section or equivalent
structured metadata with:

- selected candidate
- top rejected candidates with reasons
- deterministic score
- model score and diagnostic when model assist ran
- app/session context used for ranking
- plugin provenance where relevant
- surface target metadata where relevant

Do not emit raw model prompts, raw model completions, full memory contents, raw
thread history, or secret-bearing settings.

Optional lifecycle signals may be emitted if they follow the existing signal
style:

- `allbert.intent.candidates_collected`
- `allbert.intent.candidate_selected`
- `allbert.intent.classifier_failed`

If signals are added, they are observability only.

## Milestones

### M1: Intent Candidate Contract And Engine Skeleton

Goal: introduce the engine modules and candidate contract without changing
runtime behavior.

Implementation tasks:

- Add `AllbertAssist.Intent.Candidate` struct with all fields from the
  Candidate Model section.
- Add `AllbertAssist.Intent.Engine.decide/1` skeleton that calls through to
  the existing IntentAgent routing and wraps the result with empty candidate
  metadata.
- Add `AllbertAssist.Intent.Ranker` with deterministic scoring helpers and
  safety filter stubs.
- Add candidate redaction and bounding helpers (`redact/1`, `bound/2`).
- Add settings schema entries for candidate caps and optional model assist
  under the `intent.*` namespace.
- Review `docs/adr/0019-cross-surface-intent-enrichment.md` (created during
  the planning doc pass) and confirm its invariants are consistent with the
  M1 implementation shape. Update the ADR status from "Proposed" to
  "Accepted" when M1 is complete. Revise the ADR only if the invariants in
  the "Decision" section must change; if an invariant must be removed or
  weakened, a new ADR superseding this one is required.
- Add `AllbertAssist.Intent.EvalFixtures` module with deterministic test
  helpers used by all intent-layer tests.

Tests:

- `AllbertAssist.Intent.CandidateTest`: struct creation, validation, bounding,
  and redaction; unknown app id is rejected; unknown action name is rejected.
- Settings schema accepts all `intent.*` keys and rejects unknown or
  malformed values.
- Engine skeleton returns the same `Decision` shape as the pre-v0.19 agent
  for a direct-answer turn.

Docs:

- Update `docs/adr/0019-cross-surface-intent-enrichment.md` status to
  "Accepted" when M1 implementation is confirmed consistent with the ADR.
- Update `docs/plans/v0.19-request-flow.md` Milestone Update Log: M1 section.

Acceptance:

- Existing intent tests still pass.
- A direct answer request still produces the same high-level decision.
- Candidate metadata appears in the decision trace metadata without changing
  action execution.
- `AllbertAssist.Intent.Candidate` struct is importable and its `bound/2`
  helper enforces the caps from the Candidate Sources section.

Closeout: implemented.

### M2: Registry-Aware Candidate Collection

Goal: collect candidates from real registries rather than hand-coded app
predicates.

Implementation tasks:

- Collect action candidates from `Actions.Registry` with app id and plugin
  provenance metadata from action capabilities.
- Collect trusted and activatable skill candidates from `Skills.Registry`
  with trust, source scope, app skill path, and plugin skill root metadata.
- Collect app and surface candidates from `App.Registry` including
  `SurfaceProvider` surfaces and catalog entries.
- Include plugin provenance from registered action capability metadata and
  plugin registry where available.
- Apply collection caps from settings (defaults: 20 action, 20 skill,
  20 surface, 10 job, 10 channel, 80 total) before ranking.
- Reject but record as `:rejected` candidates that are unknown, untrusted,
  hidden, disabled, or unavailable; include a bounded `rejection_reason` for
  each.
- Wire the collected candidates into `Engine.decide/1` so `IntentAgent` can
  call the engine instead of its current hand-coded predicates. The existing
  predicate logic may remain as a scoring signal; it must not remain as the
  sole source of truth.

Tests:

- `AllbertAssist.Intent.EngineTest`: registry-driven candidate collection
  returns action, skill, and surface candidates for a neutral turn; rejected
  candidates carry reasons; total candidate count is bounded.
- Core Allbert actions appear as `:action` candidates with `:registry` source.
- `StockSageStub` (or future registered app) surfaces appear only as surface
  candidates, not executable action candidates, from neutral `active_app`.
- Plugin-contributed action candidates carry `plugin_id` in trace metadata.
- Collection caps enforce the maxima from the Candidate Sources section.

Docs:

- Update `docs/plans/v0.19-request-flow.md` Milestone Update Log: M2 section.

Acceptance:

- Core Allbert actions and skills appear as candidates in neutral context.
- `:stocksage` placeholder or future registered app surfaces appear only as
  surface/app candidates, not as executable action authority.
- Plugin-contributed action/skill candidates carry plugin provenance in trace
  metadata.
- Collection caps are applied before ranking and are overridable via
  `intent.max_candidates` and related settings.

Closeout: implemented.

### M3: Active App And Surface Ranking

Goal: use session `active_app` and registered surfaces as ranking input.

Implementation tasks:

- In `Ranker`, add app affinity scoring: when `active_app` is a known
  non-`:allbert` app, boost the score of candidates whose `app_id` matches.
- Keep neutral `:allbert` context from silently prioritizing app-specific
  actions over core Allbert ones.
- Add surface navigation candidate ranking: label/path/app text matches in
  the user's request boost matching surface candidates.
- Represent a selected surface navigation as an inert `Decision` with
  `intent: :open_surface`, no executable action, and surface metadata in
  `trace_metadata`. No dynamic route creation and no generated UI payload.
- Add app context metadata to `Decision.trace_metadata`: `active_app`, ranked
  app affinity reason, and surface target metadata when selected.
- Wire `Ranker` scoring into `Engine.decide/1` so the result replaces the
  existing agent predicates for app/surface routing.

Tests:

- `AllbertAssist.Intent.RankerTest`: app affinity boosts matching candidates
  when `active_app` is known and non-`:allbert`; neutral context does not
  boost app-specific candidates; surface navigation candidate is selected when
  the surface label/path matches.
- Engine returns an `active_app: :stocksage`-boosted decision when the
  session provides that context and StockSage-style fixtures are registered.
- Engine does NOT select a StockSage action from neutral `active_app: :allbert`
  context even if the request text could match.
- "Open Allbert chat" returns a surface navigation decision with the `/agent`
  path and no executable action.
- Unknown `active_app` string falls back to `:allbert` without atom creation.

Docs:

- Update `docs/plans/v0.19-request-flow.md` Milestone Update Log: M3 section.

Acceptance:

- A request with `active_app: :stocksage` prioritizes StockSage candidates
  when StockSage is registered (or its stub is registered).
- The same request in `active_app: :allbert` does not silently select
  StockSage app actions.
- "Open StockSage" or "open Allbert chat" returns a safe surface navigation
  suggestion when the surface is registered, without mounting routes,
  generating UI, or executing any action.
- Trace metadata records the `active_app` and ranked reasoning.

Closeout: implemented.

### M4: Preserve Risky Resource And Approval Handoff Behavior

Goal: route risky resource prompts through the enriched engine without
regressing v0.11-v0.13 safety behavior.

Implementation tasks:

- Map every existing v0.11 resource workflow to a candidate kind in the
  engine: shell → `:action` (`run_shell_command`), URL summary →
  `:action` (`summarize_url`), document inspection → `:action`
  (`inspect_document`), skill import → `:action` (`import_skill` /
  `import_local_skill`), package install → `:action`
  (`install_package`), unsupported MCP/agent URIs → `:refusal`
  candidate. Each must carry the full v0.11 `resource_access` posture
  in the candidate.
- Confirm that Approval Handoff data is passed through `Decision` unchanged;
  no new fields are required. The engine adds only `trace_metadata` for the
  candidate explanation.
- Ensure background/job-origin turns pass `foreground_or_background` and job
  metadata through the engine without forking candidate collection or policy.
- Ensure channel-origin turns carry channel metadata through the engine
  unchanged. The engine sees the same registries and posture regardless of
  which surface originated the request.
- Run the existing v0.11 risky-prompt focused tests against the enriched
  engine path. Every test must pass without modification.

Tests:

- Re-run `AllbertAssist.RuntimeIntentAgentTest` risky-prompt cases through
  the M4 engine path.
- URL summary prompt still creates a confirmation before any network fetch.
- Shell command prompt still routes to `run_shell_command` with Level 1 sandbox
  posture and confirmation requirement.
- MCP/agent URI prompt still produces an `unsupported_resource_workflow`
  decision.
- Approved confirmation resume path receives the same Approval Handoff
  structure as pre-v0.19.
- Job-origin turn produces the same intent decision as an equivalent CLI turn.
- Channel-origin turn (Telegram simulate, email simulate) produces the same
  decision as an equivalent CLI turn.

Docs:

- Update `docs/plans/v0.19-request-flow.md` Milestone Update Log: M4 section.

Acceptance:

- Existing v0.11 risky prompt tests still pass against the enriched engine.
- URL summary and document inspection prompts still require confirmation before
  network fetch.
- MCP/agent URIs still produce unsupported decisions.
- Scheduled job turns and channel turns use the same intent contract as CLI
  turns.
- No v0.11-v0.13 confirmation, resource posture, or Approval Handoff field
  is altered by the engine.

Closeout: implemented.

### M5: Optional Model-Assisted Classifier

Goal: add bounded model assistance for low-confidence cases.

Implementation tasks:

- Add `AllbertAssist.Intent.Classifier` with a single `classify/2` function
  that accepts the ranked deterministic candidates and the request text,
  calls the configured model profile through the existing AI action boundary,
  and returns `{:ok, proposal}` or `{:error, reason}`.
- Read `intent.model_assist_enabled`, `intent.model_profile`,
  `intent.model_timeout_ms`, and `intent.model_min_confidence` through
  Settings Central before any model call.
- Build the classifier prompt from a bounded, redacted candidate summary only:
  candidate `kind`, `id`, `label`, `score`, `source`, `reason`. No raw
  memory content, no raw thread history, no secrets, no trace internals.
- Validate classifier output against the already-collected candidates: unknown
  `selected_id`, mismatched `selected_kind`, non-numeric or out-of-range
  `confidence`, or invalid JSON falls back to deterministic ranking
  immediately.
- Record classifier diagnostic metadata in `Decision.trace_metadata` when the
  classifier ran: whether it succeeded, whether its proposal was used, and a
  bounded reason. Do not record raw model prompt or raw model completion.
- `AllbertAssist.Intent.Classifier.FakeClassifier` implements a
  `Classifier.Behaviour` for testing that returns a configurable proposal
  without making network calls.
- Wire classifier into `Engine.decide/1` as an optional post-ranking step
  guarded by the `intent.model_assist_enabled` check.

Tests:

- `AllbertAssist.Intent.ClassifierTest`: disabled by default (no model call
  when setting is false); fake classifier proposal selects a valid collected
  candidate; fake classifier with unknown action is rejected and deterministic
  result wins; fake classifier timeout falls back; low-confidence proposal
  falls back; invalid JSON falls back.
- Integration test: `Engine.decide/1` with fake classifier enabled and a
  valid proposal selects the proposed candidate; with invalid proposal selects
  the deterministic winner.

Docs:

- Update `docs/plans/v0.19-request-flow.md` Milestone Update Log: M5 section.

Acceptance:

- Model assistance is disabled by default.
- Invalid model output cannot invent or select unregistered actions.
- Low-confidence model output does not trigger side effects.
- Deterministic behavior remains stable when classifier fails.
- Classifier diagnostic metadata appears in traces without leaking model
  prompt or model completion text.

Closeout: implemented.

### M6: Traces, CLI/User Testing, And Release Readiness

Goal: make the new routing inspectable and manually testable end-to-end, then
close out the milestone.

Implementation tasks:

- Add trace rendering for selected/rejected candidates and app/surface context.
  The trace output should include an `## Intent Candidates` section (or
  equivalent) with: selected candidate kind/id/reason, top-N rejected
  candidates with rejection reasons, deterministic score, model classifier
  diagnostic when used, `active_app` and surface target when applicable.
- Add optional `explain_intent` and `list_intent_candidates` read-only actions
  (`:read_only` permission, `:internal` or operator-only exposure) so operators
  can inspect candidate state without running actions.
- Add focused tests for trace redaction: no raw secrets, no raw model prompts,
  no raw model completions, no full memory content, no unbounded thread history
  appear in traces or CLI output.
- Run the full operator smoke from the "Operator Smoke For Implementation"
  section of this plan end-to-end. All expected behaviors must hold.
- Update `docs/plans/v0.19-request-flow.md` Milestone Update Log with actual
  module names, command output shapes, and any behavior deferred.
- Update `CHANGELOG.md`, root `README.md`, `apps/allbert_assist/README.md`,
  `docs/plans/roadmap.md`, `AGENTS.md`, and `DEVELOPMENT.md` as described in
  the "Release Documentation Closeout" section of this plan.
- Bump `apps/allbert_assist/mix.exs` and `apps/allbert_assist_web/mix.exs`
  to `0.19.0`.
- Run the full warning gate: `mix test` focused, `mix compile --warnings-as-errors`,
  `mix format --check-formatted`, `mix credo --strict`, `mix dialyzer`,
  `mix precommit`.

Tests:

- Focused trace redaction tests: confirm no secret-like value, raw model
  output, or unbounded content appears in trace files or CLI `--trace` output.
- `explain_intent` and `list_intent_candidates` actions return bounded,
  structured output when called through `Actions.Runner.run/3`.
- End-to-end: a full CLI ask with `--trace` flag produces a trace that
  contains an Intent Candidates section with at least one selected candidate
  and one or more rejected candidates with reasons.

Docs:

- Update `docs/plans/v0.19-request-flow.md` Milestone Update Log: M6 section
  with actual modules, commands, and any deferrals.
- Complete the Release Documentation Closeout checklist in this plan.

Acceptance:

- Operators can inspect why an action, skill, surface, or refusal was selected
  by reading the trace output from any `--trace` CLI run.
- No raw secrets, raw model prompts, raw completions, or unbounded memory/thread
  text appear in traces or CLI output.
- v0.20 StockSage can rely on app/action/skill/surface metadata for routing
  rather than adding private StockSage predicates to the core intent agent.
- The full warning gate passes without errors, warnings, or Credo violations.

Closeout: implemented.

## Test Plan

Focused test files required before M6 closeout:

- `AllbertAssist.Intent.CandidateTest`
- `AllbertAssist.Intent.RankerTest`
- `AllbertAssist.Intent.EngineTest`
- `AllbertAssist.Intent.ClassifierTest`
- `AllbertAssist.Agents.IntentAgentTest`
- `AllbertAssist.RuntimeIntentAgentTest`
- app/surface routing tests with `CoreApp` and `StockSageStub`-style fixtures
- plugin provenance candidate tests
- trace redaction tests

Coverage requirements:

- direct answer route unchanged through the enriched engine
- skill/action candidate collection and selected/rejected status
- app affinity with known `active_app`
- neutral `:allbert` context does not select app-specific action without
  explicit evidence
- unknown app ids are rejected without atom creation
- registered surface navigation candidate can be selected inertly
- registered action selected from classifier output only when it was already a
  collected candidate
- unknown classifier-selected action is rejected and deterministic fallback wins
- model assist disabled by default; no model call when setting is false
- risky URL/document/package/shell/script/import workflows preserve v0.11
  resource posture and confirmation behavior unchanged
- pending confirmation/job/channel references preserve existing action paths
- traces include bounded selected/rejected candidate metadata
- traces redact secrets and omit raw model prompts/completions
- collection caps enforce the per-kind and total maximums from settings

Manual end-to-end user testing is covered in the "Operator Smoke For
Implementation" section below. Run the full smoke matrix as part of M6
acceptance.

Full gate (run after every milestone and required before M6 closeout):

- `mix test` focused files
- `mix format --check-formatted`
- `mix compile --warnings-as-errors`
- `mix credo --strict`
- `mix dialyzer`
- `mix precommit`

## Operator Smoke For Implementation

Use a disposable Allbert Home for all smoke steps. This prevents touching a
real `~/.allbert` and ensures each smoke run starts clean.

```sh
export SMOKE_HOME=$(mktemp -d /tmp/allbert-v019-smoke.XXXXXX)
```

### M1-M2 Smoke: Engine Visible In Trace

After M1-M2, the engine skeleton and registry collection are active. Verify
candidate metadata appears without changing user-visible behavior.

```sh
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --trace "what can you do?"
```

Expected:
- Normal assistant response.
- Trace output includes an `## Intent Candidates` section or equivalent.
- Selected candidate is present with a `kind`, `id`, and `reason`.
- At least one rejected candidate is present with a `rejection_reason`.
- No raw secrets, raw memory content, or raw model output in the trace.

```sh
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --trace "list my memories"
```

Expected:
- A memory-related action candidate is selected.
- Trace shows candidate `source` as `:deterministic` or `:registry`.

### M3 Smoke: App Context And Surface Navigation

```sh
# Set session active app
ALLBERT_HOME="$SMOKE_HOME" mix allbert.sessions set-active-app \
  --user local --session v019-smoke stocksage

# With active_app: :stocksage, trace should show StockSage affinity
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --trace \
  --user local --session v019-smoke "show recent analyses"
```

Expected:
- Trace shows `active_app: stocksage` as ranking context.
- StockSage-registered candidates (or `StockSageStub` surfaces) appear with
  higher scores than neutral context.
- No StockSage action is executed without explicit app action evidence.

```sh
# Neutral context: same text must not silently select StockSage
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --trace "show recent analyses"
```

Expected:
- Response is a direct answer, clarification, or safe suggestion.
- No StockSage action execution from neutral context.
- Trace shows `active_app: allbert` and no boosted StockSage candidates.

```sh
# Surface navigation
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --trace "open Allbert chat"
```

Expected:
- Response includes a safe route suggestion such as `/agent`.
- Trace shows a surface navigation candidate was selected.
- No dynamic route creation, LiveView mounting, or canvas rendering.

### M4 Smoke: Risky Resource Posture Preserved

```sh
# URL summary still requires confirmation — v0.11 posture must be unchanged
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --trace "summarize https://example.com"
```

Expected:
- Response asks for confirmation before any network fetch.
- Trace shows URL resource candidate with v0.11 resource posture fields.
- A confirmation id appears in the response.

```sh
# MCP URIs remain unsupported
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --trace "call mcp://local/resources/doc"
```

Expected:
- Response explains the workflow is unsupported.
- No MCP client, browser, or network fetch is created.
- Trace shows an unsupported/refusal candidate was selected.

```sh
# Shell command still requires confirmation
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --trace "run ls -la in my home directory"
```

Expected:
- Response requests confirmation before execution.
- Trace shows the shell action candidate with Level 1 sandbox posture.

### M4 Smoke: Channel-Origin Intent Consistency

```sh
ALLBERT_HOME="$SMOKE_HOME" mix allbert.channels telegram simulate \
  --external-user 123 --chat 456 "summarize https://example.com"
```

Expected:
- Channel-origin turn produces the same URL confirmation posture as the CLI turn.

```sh
ALLBERT_HOME="$SMOKE_HOME" mix allbert.channels email simulate \
  --external-user alice@example.com "open Allbert chat"
```

Expected:
- Channel-origin turn returns the same surface navigation suggestion as the
  CLI turn.

### M5 Smoke: Model Classifier Disabled By Default

```sh
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings get intent.model_assist_enabled
```

Expected:
- Value is `false`.
- No model call occurs during intent routing when this setting is off.

```sh
ALLBERT_HOME="$SMOKE_HOME" mix allbert.settings get intent.max_candidates
```

Expected:
- Value is `80` (or the configured default).

### M6 Smoke: Trace Inspection And Explain Intent

```sh
# Verify trace file contains candidate metadata
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask --trace "list my memories"
# Note the trace path printed in the response
# Open that trace file and confirm the Intent Candidates section is present,
# has selected and rejected entries, and contains no raw secrets or unbounded content.
ls "$SMOKE_HOME/memory/traces/"
```

Expected:
- Trace file contains structured candidate metadata.
- Selected candidate has a bounded reason.
- Rejected candidates have bounded rejection reasons.
- No API key, raw model prompt, full memory content, or raw thread history.

```sh
# Explain intent action (if implemented in M6)
ALLBERT_HOME="$SMOKE_HOME" mix allbert.ask "explain intent for: list my memories"
```

Expected (if `explain_intent` action is registered):
- Response returns a bounded, read-only explanation of why the route was chosen.
- No side effects run.

### Cleanup

```sh
rm -rf "$SMOKE_HOME"
```

## Release Documentation Closeout

M6 completes the release-documentation pass for user testing:

- `CHANGELOG.md` has a `v0.19 - Cross-Surface Intent Enrichment` section with
  status, added behavior, changed behavior, safety notes, and verification
  summary.
- Root `README.md` describes v0.19 intent engine behavior: `mix allbert.ask
  --trace` shows Intent Candidates in the trace, `intent.model_assist_enabled`
  is the model-assist toggle, and `active_app` routing is context-only.
- `apps/allbert_assist/README.md` lists the new intent modules:
  `AllbertAssist.Intent.Engine`, `AllbertAssist.Intent.Candidate`,
  `AllbertAssist.Intent.Ranker`, `AllbertAssist.Intent.Classifier`.
- `docs/plans/roadmap.md` marks v0.19 implemented and ready for operator
  manual verification.
- `docs/plans/v0.19-request-flow.md` Milestone Update Log records actual
  module names, CLI command output shapes, and any behavior deferred from
  the original plan.
- `docs/plans/v0.20-plan.md` confirms StockSage can rely on the engine's
  app/action/skill/surface registry metadata rather than core intent-agent
  special cases.
- `AGENTS.md` and `DEVELOPMENT.md` now treat v0.19 as regression context and
  reference ADR 0019.
- `apps/allbert_assist/mix.exs` and `apps/allbert_assist_web/mix.exs` are
  bumped to `0.19.0`.
- Expected release tag is `v0.19`. No release tag should be created before
  operator acceptance after user testing.

Release documentation must preserve the safety story:

- v0.19 adds registry-aware candidate collection, deterministic scoring, app
  affinity ranking, surface navigation candidates, and optional model-assisted
  classification.
- v0.19 does not add new shell, script, package, import, network, MCP, or
  agent delegation powers.
- Model-assisted classification is disabled by default; when enabled it can
  only rank already-collected registered candidates, never invent new ones.
- `active_app` influences candidate ranking but does not grant execution
  authority or bypass Security Central.
- All v0.11 risky resource posture and confirmation behavior is preserved
  unchanged.

## Non-Goals

- No new shell, script, package, network, import, browser, crawler, MCP, or
  agent delegation powers.
- No dynamic route creation.
- No generated UI, ephemeral UI, workspace shell, canvas rendering, AG-UI, or
  A2UI emission.
- No StockSage domain implementation or StockSage-specific core predicates.
- No reviewed-memory retrieval, embeddings, vector search, or automatic memory
  promotion.
- No autonomous skill/app/plugin creation.
- No channel-specific intent fork.
- No model-selected side effect without registry validation, Security Central,
  confirmation where required, and action runner execution.

## Definition Of Done

v0.19 is done when:

- ADR 0019 `docs/adr/0019-cross-surface-intent-enrichment.md` exists and is
  referenced from AGENTS.md and DEVELOPMENT.md.
- Runtime turns pass through `AllbertAssist.Intent.Engine.decide/1`.
- All M1-M6 acceptance criteria pass.
- All M1-M6 focused tests pass.
- Existing v0.11 risky-resource focused tests pass against the enriched
  engine path without modification.
- Existing v0.18 app/surface contract tests pass unchanged.
- Skill, action, job, channel, app, and surface selection is registry-aware,
  security-aware, and traceable.
- `active_app` changes candidate ranking but not authorization or Security
  Central decisions.
- Registered surfaces can appear as inert navigation candidates without
  dynamic route creation or UI generation.
- Optional model-assisted classification is setting-gated, bounded, validated,
  and covered by fake-classifier tests (no real model calls in tests).
- Intent traces explain selected and rejected candidates in a bounded,
  redacted section without leaking secrets, raw model prompts, or raw
  completions.
- The operator smoke from "Operator Smoke For Implementation" passes
  end-to-end.
- v0.20 StockSage can plug into routing through plugin/app/action/skill/surface
  metadata rather than core intent-agent special cases.
- The Release Documentation Closeout checklist is complete.
- `apps/allbert_assist` and `apps/allbert_assist_web` are both at `0.19.0`.
- The full warning gate (`mix compile --warnings-as-errors`, `mix credo
  --strict`, `mix dialyzer`, `mix precommit`) passes clean.
